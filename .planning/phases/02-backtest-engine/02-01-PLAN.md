---
phase: 02-backtest-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/backtest.py]
autonomous: true
requirements: [BT-01, BT-03]
must_haves:
  truths:
    - "scripts/backtest.py가 봇 프로세스 없이 단독 실행된다 (python scripts/backtest.py --self-test)"
    - "backtest.py가 alt_monitor.py나 bithumb/client.py를 import하지 않는다"
    - "DataSlice가 커서 이후 인덱스 접근 시 IndexError를 던진다"
    - "load_events()가 pump_log를 읽기 전용으로 조회해 이벤트 목록을 반환한다"
  artifacts:
    - path: "scripts/backtest.py"
      provides: "백테스트 스크립트 골격 + 전략 상수 + DataSlice + load_events + --self-test"
      min_lines: 90
  key_links:
    - from: "scripts/backtest.py"
      to: "bithumb.db (sqlite3 SELECT)"
      via: "load_events() 읽기 전용 쿼리"
      pattern: "SELECT.*pump_log"
    - from: "scripts/backtest.py DataSlice.__getitem__"
      to: "IndexError"
      via: "커서 초과 인덱스 접근 시 raise"
      pattern: "raise IndexError"
---

<objective>
백테스트 스크립트 `scripts/backtest.py`의 골격을 만든다: 전략 상수 블록, lookahead를 물리적으로 차단하는 `DataSlice` 래퍼 클래스, `pump_log` 이벤트를 읽기 전용으로 조회하는 `load_events()`, 그리고 코드만으로 검증 가능한 `--self-test` 모드.

Purpose: 시뮬레이션·리포트 로직(Plan 02·03)이 의존하는 계약(상수, DataSlice 인터페이스, 이벤트 로딩)을 먼저 확정한다. 봇 코드와 완전 분리(BT-01)되고 lookahead가 코드로 강제(BT-03)됨을 self-test로 증명한다.
Output: `scripts/backtest.py` — 골격 + DataSlice + load_events + self-test. 아직 시뮬레이션 루프는 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backtest-engine/02-CONTEXT.md
@.planning/phases/02-backtest-engine/02-RESEARCH.md

<interfaces>
<!-- bithumb/db.py — 백테스트가 의존하는 동결 계약. 그대로 사용, 재구현 금지. -->

get_ticks(pump_id: int) -> list[dict]
  특정 펌핑 이벤트의 모든 틱을 seq 순으로 반환. 존재하지 않는 pump_id는 빈 리스트.
  각 dict 키: id, pump_id, seq, exchange_ts, recv_ts, price, acc_value,
              volume_power, gap_before, ts_estimated

pump_ticks 테이블 컬럼:
  id, pump_id, seq, exchange_ts (REAL, epoch sec), recv_ts (REAL),
  price (REAL), acc_value, volume_power, gap_before (INTEGER 0/1),
  ts_estimated (INTEGER 0/1)

pump_log 테이블 컬럼:
  id, detected_at (TEXT), coin (TEXT), base_price (REAL), pump_pct,
  vol_mult, price_1m..price_5m, peak_price, peak_at_sec, max_drop_pct,
  pullback_2pct, bounce_after, entered

bithumb/db.py 패턴 (L94-98):
  DB_PATH — sqlite db 경로 상수 (data/trades.db)
  _conn() — sqlite3.connect + row_factory = sqlite3.Row 컨텍스트 매니저
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 스크립트 골격 + 전략 상수 + load_events()</name>
  <read_first>
    - scripts/show_pnl.py (기존 분석 스크립트의 sys.path.insert + sqlite3.Row + stdout 패턴)
    - scripts/show_stats.py (단일 파일 분석 스크립트 구조)
    - bithumb/db.py L60-98 (pump_log/pump_ticks 스키마, _conn, DB_PATH)
  </read_first>
  <files>scripts/backtest.py</files>
  <action>
    `scripts/backtest.py`를 신규 생성한다. 다음 구조를 정확히 작성:

    1. 모듈 docstring: `"""빗썸 펌핑 눌림목 전략 백테스트 — 틱 재생 기반 오프라인 시뮬레이션 (읽기 전용)."""`

    2. import 블록 (이 줄들만 — `alt_monitor`·`bithumb.client` 절대 import 금지):
       ```python
       import argparse
       import sqlite3
       import sys
       from pathlib import Path

       sys.path.insert(0, str(Path(__file__).parent.parent))
       from bithumb.db import DB_PATH
       ```

    3. 전략 상수 블록 (파일 상단, 주석에 D-08/BT-04 표기). 이 값들을 정확히 사용:
       ```python
       # ── 전략 상수 (D-08: Phase 2는 고정 1조합. Phase 3가 그리드 서치로 파라미터화) ──
       ENTRY_DROP_PCT   = 0.07    # D-02: 주행 고점 대비 -7% 눌림에서 진입
       TP_PCT           = 0.05    # D-06: 익절 +5%
       SL_PCT           = 0.03    # D-06: 손절 -3%
       TIMEOUT_SEC      = 600     # D-06: 시간초과 청산 (10분 = 이벤트 길이)
       MIN_TICKS        = 4       # 이 미만의 틱을 가진 이벤트는 스킵 (Discretion)
       GAP_EXCLUDE_PCT  = 0.30    # D-15: 갭 비율이 30% 초과면 이벤트 통째 제외

       # ── 비용 모델 (BT-04) ──
       ROUND_TRIP_FEE   = 0.005                       # 왕복 수수료 0.5%
       SLIPPAGE_SCENARIOS = (0.0, 0.005, 0.01, 0.02)  # D-11: 항상 4행
       ```

    4. `load_events(db_path) -> list[dict]` 함수 — 읽기 전용 SELECT만:
       ```python
       def load_events(db_path) -> list[dict]:
           """백테스트 대상 펌핑 이벤트 목록. pump_ticks 행이 있는 이벤트만, detected_at 순.

           읽기 전용 — SELECT 외 어떤 쿼리도 실행하지 않는다 (BT-01).
           """
           con = sqlite3.connect(db_path)
           con.row_factory = sqlite3.Row
           try:
               rows = con.execute(
                   """
                   SELECT p.id, p.coin, p.base_price, p.pump_pct, p.detected_at,
                          COUNT(t.id) AS tick_count
                   FROM pump_log p
                   JOIN pump_ticks t ON t.pump_id = p.id
                   GROUP BY p.id
                   HAVING tick_count >= 1
                   ORDER BY p.detected_at
                   """
               ).fetchall()
           finally:
               con.close()
           return [dict(r) for r in rows]
       ```

    5. 파일 끝에 `if __name__ == "__main__":` 진입점 — 지금은 argparse로 `--self-test` 플래그만 받고, self-test가 아니면 "Plan 02·03에서 구현 예정" 메시지를 출력하고 종료. (`--db` 인자도 정의하되 기본값 `DB_PATH`.)

    네이밍: snake_case 함수, UPPERCASE 상수, 타입힌트 필수, 4-space 들여쓰기. 봇 코드(alt_monitor.py 등)는 일절 수정 금지.
  </action>
  <verify>
    <automated>python scripts/backtest.py 2>&1 | head -5 ; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/backtest.py` 파일이 존재한다
    - `grep -nE "(import alt_monitor|from scripts.alt_monitor|bithumb.client|bithumb\.client)" scripts/backtest.py` 가 0건 (봇 코드 import 없음)
    - `grep -c "ENTRY_DROP_PCT = 0.07" scripts/backtest.py` 가 1
    - `grep -c "ROUND_TRIP_FEE = 0.005" scripts/backtest.py` 가 1
    - `grep -c "SLIPPAGE_SCENARIOS = (0.0, 0.005, 0.01, 0.02)" scripts/backtest.py` 가 1
    - `grep -c "def load_events" scripts/backtest.py` 가 1
    - load_events 함수 본문에 `SELECT` 가 있고 `INSERT`/`UPDATE`/`DELETE`/`CREATE` 는 없다
    - `python scripts/backtest.py` 실행이 예외 없이 종료 (exit code 0)
  </acceptance_criteria>
  <done>backtest.py가 상수·load_events·argparse 골격을 갖추고 봇 코드 import 없이 단독 실행된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DataSlice lookahead 강제 클래스 + --self-test 검증</name>
  <read_first>
    - scripts/backtest.py (Task 1에서 만든 현재 상태)
    - .planning/phases/02-backtest-engine/02-RESEARCH.md §"Pattern 2: DataSlice lookahead 강제 패턴" (L151-198, 정본 구현 예시)
  </read_first>
  <files>scripts/backtest.py</files>
  <behavior>
    - 커서 0인 DataSlice: `len()` == 1, `current` == ticks[0], `[0]` == ticks[0]
    - `advance()` 호출 후 커서 1: `len()` == 2, `[1]` 접근 정상
    - 커서를 넘는 인덱스 접근(`slice[cursor+1]`, 미래 인덱스)은 `IndexError` 발생
    - 음수 인덱스 `[-1]`은 현재 커서 틱(노출분 마지막)을 반환, 노출범위 밖 음수는 IndexError
    - `visible()`는 0..cursor 까지의 틱 리스트만 반환
  </behavior>
  <action>
    `scripts/backtest.py`에 `DataSlice` 클래스를 추가한다 (load_events 위 또는 아래, 상수 블록 다음). 02-RESEARCH.md L163-197의 구현을 정확히 사용:

    ```python
    class DataSlice:
        """현재 커서까지의 틱만 노출. 미래 인덱스 접근 시 IndexError (D-14, BT-03).

        진입·청산 판정 함수는 원본 list[dict]이 아니라 이 객체만 받는다 —
        미래 데이터 접근을 물리적으로 차단한다.
        """
        def __init__(self, ticks: list[dict]):
            self._ticks = ticks      # 원본 (판정 로직에 직접 노출 안 함)
            self._cursor = 0         # 현재 재생 위치 (0-based, 포함)

        def advance(self) -> None:
            self._cursor += 1

        @property
        def cursor(self) -> int:
            return self._cursor

        @property
        def current(self) -> dict:
            return self._ticks[self._cursor]

        def __len__(self) -> int:
            return self._cursor + 1

        def __getitem__(self, i: int) -> dict:
            idx = self._cursor + 1 + i if i < 0 else i
            if idx > self._cursor or idx < 0:
                raise IndexError(
                    f"lookahead 위반: 인덱스 {i} -> 절대 {idx}, 커서는 {self._cursor}"
                )
            return self._ticks[idx]

        def visible(self) -> list[dict]:
            return self._ticks[: self._cursor + 1]
    ```

    그리고 `_self_test() -> bool` 함수를 추가해 DataSlice를 직접 검증한다:
    - 합성 틱 리스트 생성 (예: `[{"price": p} for p in range(10)]`)
    - DataSlice 생성 → `len()==1`, `current["price"]==0`, `[0]` 정상 확인
    - 커서 5까지 advance → `len()==6`, `[5]` 정상 확인
    - `try: slice[6]` 가 `IndexError`를 던지는지 확인 (안 던지면 self-test 실패)
    - `try: slice[10]` (먼 미래)가 `IndexError`를 던지는지 확인
    - `[-1]`이 ticks[5]를 반환하는지 확인
    - 모든 검증 통과 시 "self-test PASS" 출력 후 True, 실패 시 어떤 검증이 깨졌는지 출력 후 False

    `if __name__ == "__main__":` 블록을 수정: `--self-test` 플래그가 있으면 `_self_test()` 실행 후 결과에 따라 `sys.exit(0 if ok else 1)`.
  </action>
  <verify>
    <automated>python scripts/backtest.py --self-test ; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class DataSlice" scripts/backtest.py` 가 1
    - `grep -c "raise IndexError" scripts/backtest.py` 가 1 이상
    - `grep -c "def _self_test" scripts/backtest.py` 가 1
    - `python scripts/backtest.py --self-test` 가 exit code 0 으로 종료하고 "self-test PASS" 를 출력한다
    - DataSlice가 미래 인덱스(커서 초과) 접근 시 IndexError를 던짐 — self-test가 이를 검증하고 통과
  </acceptance_criteria>
  <done>DataSlice가 lookahead를 코드로 차단하고, --self-test가 IndexError 동작을 자동 증명하며 PASS한다.</done>
</task>

</tasks>

<verification>
- `python scripts/backtest.py --self-test` 가 PASS (exit 0) — BT-03 lookahead 차단 증명
- `python scripts/backtest.py` 가 예외 없이 종료 — 봇 분리 단독 실행 확인 (BT-01)
- `grep -E "(alt_monitor|bithumb.client)" scripts/backtest.py` 가 비어 있음 — 봇 코드 import 없음 (BT-01)
- load_events 본문에 SELECT만 존재, 쓰기 쿼리 없음 — DB 읽기 전용 (BT-01)
</verification>

<success_criteria>
- scripts/backtest.py가 봇 프로세스/모듈 없이 단독 실행된다 (로드맵 성공기준 1)
- DataSlice 미래 인덱스 접근 시 런타임 에러(IndexError) 발생 (로드맵 성공기준 3)
- 전략 상수가 파일 상단 1조합으로 고정됨 (D-08)
</success_criteria>

<output>
After completion, create `.planning/phases/02-backtest-engine/02-01-SUMMARY.md`
</output>

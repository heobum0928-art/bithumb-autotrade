---
phase: 02-backtest-engine
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [scripts/backtest.py]
autonomous: true
requirements: [BT-02, BT-04]
must_haves:
  truths:
    - "simulate_event()가 한 이벤트의 틱을 시간순으로 한 틱씩 재생한다"
    - "진입은 주행 고점 대비 -7% 눌림 시 충족되고, 체결가는 충족 틱의 다음 틱 price다"
    - "청산은 TP/SL/시간초과 3종으로, TP/SL 체결가도 돌파 틱의 다음 틱 price다"
    - "gap_before=1 틱에서는 TP/SL 돌파 판정을 건너뛴다"
    - "수수료(왕복 0.5%)와 슬리피지가 손익 계산에 반영된다"
  artifacts:
    - path: "scripts/backtest.py"
      provides: "_apply_slip, _net_pnl_pct, simulate_event 시뮬레이션 함수"
      contains: "def simulate_event"
  key_links:
    - from: "scripts/backtest.py simulate_event"
      to: "DataSlice"
      via: "재생 루프가 DataSlice.advance로 커서를 전진시킨다"
      pattern: "DataSlice"
    - from: "scripts/backtest.py simulate_event"
      to: "체결가"
      via: "다음 틱(cursor+1) price로 진입/청산 체결 (D-04/D-07)"
      pattern: "cursor \\+ 1"
---

<objective>
`scripts/backtest.py`에 틱 재생 시뮬레이션을 추가한다: 한 펌핑 이벤트의 틱을 시간순으로 재생하며 눌림목 진입을 판정하고, TP/SL/시간초과로 가상 포지션을 청산한다. 수수료·슬리피지를 체결가/손익에 반영한다.

Purpose: BT-02(시간순 재생 시뮬레이션)와 BT-04(수수료·슬리피지 모델)를 구현한다. lookahead 방지(진입/청산 모두 "다음 틱" 체결, 갭 틱 판정 제외)가 코드에 강제된다.
Output: `simulate_event()` + 비용 헬퍼 함수. 이벤트 1건을 받아 Trade dict 또는 None을 반환.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-backtest-engine/02-CONTEXT.md
@.planning/phases/02-backtest-engine/02-RESEARCH.md
@.planning/phases/02-backtest-engine/02-01-SUMMARY.md
@scripts/backtest.py

<interfaces>
<!-- Plan 01이 만든 backtest.py 계약 -->

상수: ENTRY_DROP_PCT=0.07, TP_PCT=0.05, SL_PCT=0.03, TIMEOUT_SEC=600,
      MIN_TICKS=4, GAP_EXCLUDE_PCT=0.30, ROUND_TRIP_FEE=0.005,
      SLIPPAGE_SCENARIOS=(0.0, 0.005, 0.01, 0.02)

class DataSlice(ticks: list[dict])
  .advance() -> None          # 커서 +1
  .cursor -> int              # 현재 재생 위치
  .current -> dict            # 현재 커서 틱
  .__len__() -> int           # 노출된 틱 수 (cursor+1)
  .__getitem__(i) -> dict     # i > cursor 시 IndexError
  .visible() -> list[dict]

틱 dict 키: seq, exchange_ts, recv_ts, price, acc_value, volume_power,
            gap_before (0/1), ts_estimated (0/1)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 비용 헬퍼 (_apply_slip, _net_pnl_pct)</name>
  <read_first>
    - scripts/backtest.py (Plan 01 결과 — 상수 블록 위치 확인)
    - .planning/phases/02-backtest-engine/02-RESEARCH.md §"Pattern 4: 수수료·슬리피지 손익 모델" (L249-267)
  </read_first>
  <files>scripts/backtest.py</files>
  <behavior>
    - `_apply_slip(1000, 0.01, "buy")` == 1010.0 (매수는 비싸게)
    - `_apply_slip(1000, 0.01, "sell")` == 990.0 (매도는 싸게)
    - `_apply_slip(1000, 0.0, "buy")` == 1000.0 (슬리피지 0이면 변화 없음)
    - `_net_pnl_pct(100, 110)` == 0.095 (gross 0.10 − 수수료 0.005)
    - `_net_pnl_pct(100, 100)` == -0.005 (무변동도 왕복 수수료만큼 손실)
  </behavior>
  <action>
    `scripts/backtest.py`에 비용 헬퍼 두 함수를 추가한다 (상수 블록 다음, DataSlice 부근). 02-RESEARCH.md L260-265의 정의를 정확히 사용:

    ```python
    def _apply_slip(price: float, slippage: float, side: str) -> float:
        """슬리피지를 체결가에 반영. 매수는 비싸게(+), 매도는 싸게(-)."""
        return price * (1 + slippage) if side == "buy" else price * (1 - slippage)


    def _net_pnl_pct(entry: float, exit_price: float) -> float:
        """진입가, 청산가(슬리피지 이미 반영됨) -> 왕복 수수료 차감 순손익률."""
        gross = (exit_price - entry) / entry
        return gross - ROUND_TRIP_FEE
    ```

    모델 규칙 (이중 계산 금지): 슬리피지는 `_apply_slip`으로 체결가에만 반영하고, 수수료는 `_net_pnl_pct`에서 손익률에만 반영한다. 슬리피지를 손익률에 또 빼거나 수수료를 체결가에 또 넣지 않는다.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'scripts'); from backtest import _apply_slip,_net_pnl_pct; assert _apply_slip(1000,0.01,'buy')==1010.0; assert _apply_slip(1000,0.01,'sell')==990.0; assert abs(_net_pnl_pct(100,110)-0.095)<1e-9; assert abs(_net_pnl_pct(100,100)+0.005)<1e-9; print('cost helpers OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def _apply_slip" scripts/backtest.py` 가 1
    - `grep -c "def _net_pnl_pct" scripts/backtest.py` 가 1
    - 위 verify 명령이 "cost helpers OK"를 출력하고 exit 0
    - `_net_pnl_pct`가 `ROUND_TRIP_FEE` 상수를 참조한다 (`grep "ROUND_TRIP_FEE" scripts/backtest.py` 가 2건 이상)
  </acceptance_criteria>
  <done>슬리피지·수수료 헬퍼가 명세된 값을 정확히 계산하고, 슬리피지/수수료가 이중 계산되지 않는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: simulate_event() 틱 재생 + 진입/청산 시뮬레이션</name>
  <read_first>
    - scripts/backtest.py (Task 1 이후 상태 — 상수, DataSlice, 비용 헬퍼)
    - .planning/phases/02-backtest-engine/02-RESEARCH.md §"Pattern 3: 틱 재생 루프 구조" (L200-247, 의사코드 정본)
    - .planning/phases/02-backtest-engine/02-CONTEXT.md D-02~D-09 (진입/청산 잠긴 결정)
  </read_first>
  <files>scripts/backtest.py</files>
  <behavior>
    - 틱 < MIN_TICKS(4) 이벤트: `simulate_event`가 `None` 반환 (데이터 부족 스킵)
    - 진입 없는 이벤트(고점 대비 -7% 눌림 미발생): `None` 반환
    - TP 케이스: 진입 후 가격이 +5% 이상 → exit_reason="익절", pnl_pct > 0
    - SL 케이스: 진입 후 가격이 -3% 이하 → exit_reason="손절", pnl_pct < 0
    - 시간초과 케이스: TP/SL 미도달, 틱 소진 → exit_reason="시간초과", 마지막 틱 price로 청산
    - 갭 케이스: gap_before=1 틱에서 가격이 TP를 넘어도 그 틱에서는 청산 안 함, 다음 정상 틱에서 판정 재개
    - 진입 조건이 마지막 틱에서 충족: 다음 틱 없음 → 무진입 처리(None)
  </behavior>
  <action>
    `scripts/backtest.py`에 `simulate_event(ticks: list[dict], slippage: float) -> dict | None`를 추가한다. 02-RESEARCH.md L206-245의 의사코드를 따르되, 다음 사항을 정확히 구현:

    1. 시그니처: `def simulate_event(ticks: list[dict], slippage: float) -> dict | None`. 전략 상수는 모듈 상수(ENTRY_DROP_PCT 등)를 직접 참조.

    2. 가드: `if len(ticks) < MIN_TICKS: return None`.

    3. 재생: `sl = DataSlice(ticks)`, `running_peak = ticks[0]["price"]`, `state = "WAITING_ENTRY"`, `entry = None`.
       `while sl.cursor < len(ticks):` 루프, 매 반복 `tick = sl.current` 로 현재 틱 읽고 끝에서 `sl.advance()`.

    4. 매 틱 `running_peak = max(running_peak, tick["price"])` (D-02 동적 고점, 미래 아님).

    5. WAITING_ENTRY 상태 (D-02/D-03/D-04):
       - `drawdown = (tick["price"] - running_peak) / running_peak`
       - `drawdown <= -ENTRY_DROP_PCT` 면 진입 조건 충족
       - 다음 틱 없음 가드: `if sl.cursor + 1 >= len(ticks): return None` (마지막 틱에서 충족 시 무진입)
       - 체결: `fill = ticks[sl.cursor + 1]["price"]`, `entry_price = _apply_slip(fill, slippage, "buy")`
       - `entry = {"price": entry_price, "tick_idx": sl.cursor + 1, "ts": ticks[sl.cursor + 1]["exchange_ts"]}`
       - `state = "IN_POSITION"`

    6. IN_POSITION 상태 (D-06/D-07/D-09):
       - D-09: `if tick["gap_before"] == 1:` 면 TP/SL 돌파 판정 건너뜀 (running_peak 갱신만, 이미 위에서 함)
       - 정상 틱: `chg = (tick["price"] - entry["price"]) / entry["price"]`
       - `hit = "익절" if chg >= TP_PCT else ("손절" if chg <= -SL_PCT else None)`
       - hit가 있고 `sl.cursor + 1 < len(ticks)` 면 다음 틱으로 청산 (D-07):
         - `exit_fill = ticks[sl.cursor + 1]["price"]`, `exit_price = _apply_slip(exit_fill, slippage, "sell")`
         - Trade dict를 만들어 return (아래 7번 형식)

    7. 시간초과 청산 (D-06): 루프 종료 후 `state == "IN_POSITION"` 이면 마지막 틱으로 강제 청산:
       - `exit_fill = ticks[-1]["price"]`, `exit_price = _apply_slip(exit_fill, slippage, "sell")`, `reason = "시간초과"`

    8. Trade dict 반환 형식 (청산 시 공통). 필드:
       ```python
       {
           "entry_ts":    entry["ts"],
           "entry_price": entry["price"],
           "exit_ts":     exit_tick["exchange_ts"],
           "exit_price":  exit_price,
           "exit_reason": reason,
           "hold_sec":    int(exit_tick["exchange_ts"] - entry["ts"]),
           "pnl_pct":     _net_pnl_pct(entry["price"], exit_price),
           "slippage":    slippage,
       }
       ```
       진입 못 한 경우 `return None`.

    9. 안티패턴 금지: 같은 틱에서 진입+청산 즉시 체결 금지(다음 틱 분리), 갭 틱 TP/SL 판정 금지, 이벤트 전체 최고가 사전 계산 금지(running_peak는 재생 중 누적).

    `_close()` 같은 내부 헬퍼로 8번 Trade dict 생성을 분리해도 좋다 (Discretion).
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'scripts'); from backtest import simulate_event; T=lambda s,p,g=0: {'seq':s,'exchange_ts':1000+s*10,'recv_ts':1000+s*10,'price':p,'gap_before':g}; assert simulate_event([T(0,100),T(1,101)],0.0) is None, 'MIN_TICKS skip'; tp=[T(0,100),T(1,110),T(2,101),T(3,116),T(4,117)]; r=simulate_event(tp,0.0); assert r and r['exit_reason']=='익절', r; sl=[T(0,100),T(1,110),T(2,101),T(3,96),T(4,95)]; r2=simulate_event(sl,0.0); assert r2 and r2['exit_reason']=='손절', r2; print('simulate_event OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def simulate_event" scripts/backtest.py` 가 1
    - `grep -c "DataSlice" scripts/backtest.py` 가 2건 이상 (정의 + simulate_event 내 사용)
    - `grep -c "gap_before" scripts/backtest.py` 가 1건 이상 (D-09 갭 틱 판정 제외)
    - simulate_event 본문에 `cursor + 1` 패턴이 존재 (다음 틱 체결, D-04/D-07)
    - 위 verify 명령이 "simulate_event OK"를 출력하고 exit 0 — MIN_TICKS 스킵·익절·손절 케이스 검증
    - `python scripts/backtest.py --self-test` 가 여전히 exit 0 (Plan 01 self-test 회귀 없음)
  </acceptance_criteria>
  <done>simulate_event가 틱을 시간순 재생해 눌림목 진입·TP/SL/시간초과 청산을 시뮬레이션하고, "다음 틱" 체결과 갭 틱 판정 제외를 강제한다.</done>
</task>

</tasks>

<verification>
- verify 명령으로 MIN_TICKS 스킵·익절·손절 케이스가 자동 검증됨 (BT-02)
- simulate_event가 `cursor + 1` 다음 틱 체결을 사용 — lookahead 방지 (BT-03 보강)
- `_apply_slip`/`_net_pnl_pct`가 슬리피지·수수료를 손익에 반영 (BT-04)
- `grep gap_before scripts/backtest.py` 로 D-09 갭 틱 판정 제외 확인
</verification>

<success_criteria>
- 틱을 시간순으로 재생하며 매 틱 손절/익절 조건을 검사한다 — 스냅샷만으로 청산 결정하지 않음 (로드맵 성공기준 2)
- 수수료(왕복 0.5%)·슬리피지가 손익 계산에 반영됨 (로드맵 성공기준 4 일부, BT-04)
- 진입/청산 모두 "다음 틱" 체결, 갭 틱 판정 제외 (D-04/D-07/D-09)
</success_criteria>

<output>
After completion, create `.planning/phases/02-backtest-engine/02-02-SUMMARY.md`
</output>

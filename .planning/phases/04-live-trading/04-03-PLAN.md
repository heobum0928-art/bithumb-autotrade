---
phase: 04-live-trading
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - scripts/watchdog.py
  - docs/STRATEGY.md
autonomous: true
requirements:
  - VB-05

must_haves:
  truths:
    - "watchdog.py BOTS dict에 'vb_trader' 항목이 있고 EXTRA_ARGS에 ['--dry-run']이 등록된다"
    - "watchdog 실행 후 vb_trader 프로세스가 --dry-run 인자로 시작된다"
    - "docs/STRATEGY.md의 파라미터 변경 이력 테이블에 VB 전략 도입 항목이 추가된다"
  artifacts:
    - path: "scripts/watchdog.py"
      provides: "vb_trader watchdog 통합"
      contains: "vb_trader"
    - path: "docs/STRATEGY.md"
      provides: "VB 전략 파라미터 기록"
      contains: "vb_trader"
  key_links:
    - from: "scripts/watchdog.py BOTS"
      to: "scripts/vb_trader.py"
      via: "subprocess 실행"
      pattern: "vb_trader.*vb_trader.py"
    - from: "scripts/watchdog.py EXTRA_ARGS"
      to: "--dry-run 플래그"
      via: "EXTRA_ARGS['vb_trader']"
      pattern: "vb_trader.*dry-run"
---

<objective>
watchdog.py에 vb_trader를 --dry-run 모드로 관리하는 항목을 추가하고, CLAUDE.md 규칙에 따라 docs/STRATEGY.md에 VB 전략 도입 이력을 기록한다.

Purpose: 봇 재시작 감시를 완성해 vb_trader가 장애 후 자동 복구되게 한다. STRATEGY.md 기록은 CLAUDE.md 필수 규칙 준수.
Output: watchdog.py에 vb_trader 항목 추가, docs/STRATEGY.md 업데이트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-live-trading/04-CONTEXT.md
@.planning/phases/04-live-trading/04-01-SUMMARY.md
@.planning/phases/04-live-trading/04-02-SUMMARY.md

<interfaces>
<!-- watchdog.py 현재 구조 -->

From scripts/watchdog.py (lines 36-64):
```python
BOTS = {
    "alt_monitor":           ROOT / "scripts" / "alt_monitor.py",
    "tg_bot":                ROOT / "scripts" / "tg_bot.py",
    "claude_intelligence":   ROOT / "scripts" / "claude_intelligence.py",
    "swing_monitor":         ROOT / "scripts" / "swing_monitor.py",
}

EXTRA_ARGS: dict[str, list[str]] = {
    "claude_screener_dry":   ["--dry-run"],
    "claude_screener_watch": ["--watch-mode"],
    "swing_monitor":         ["--loop"],
}

KILL_KEYWORDS: dict[str, str] = {
    "claude_screener_dry":   "--dry-run",
    "claude_screener_watch": "--watch-mode",
}
```

추가할 내용:
- BOTS에: `"vb_trader": ROOT / "scripts" / "vb_trader.py"`
- EXTRA_ARGS에: `"vb_trader": ["--dry-run"]`
- KILL_KEYWORDS에: `"vb_trader": "--dry-run"` (동일 스크립트를 dry/live로 구별하기 위해)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: watchdog.py에 vb_trader 항목 추가</name>
  <files>scripts/watchdog.py</files>
  <read_first>
    - scripts/watchdog.py (전체 파일 — BOTS, EXTRA_ARGS, KILL_KEYWORDS dict 위치 파악)
  </read_first>
  <action>
scripts/watchdog.py의 세 dict에 각각 vb_trader 항목을 추가한다.

**1. BOTS dict에 추가:**
```python
BOTS = {
    "alt_monitor":           ROOT / "scripts" / "alt_monitor.py",
    "tg_bot":                ROOT / "scripts" / "tg_bot.py",
    "claude_intelligence":   ROOT / "scripts" / "claude_intelligence.py",
    "swing_monitor":         ROOT / "scripts" / "swing_monitor.py",
    "vb_trader":             ROOT / "scripts" / "vb_trader.py",   # VB 전략 dry-run
}
```

**2. EXTRA_ARGS dict에 추가:**
```python
EXTRA_ARGS: dict[str, list[str]] = {
    "claude_screener_dry":   ["--dry-run"],
    "claude_screener_watch": ["--watch-mode"],
    "swing_monitor":         ["--loop"],
    "vb_trader":             ["--dry-run"],   # 모의투자 모드로 시작
}
```

**3. KILL_KEYWORDS dict에 추가:**
```python
KILL_KEYWORDS: dict[str, str] = {
    "claude_screener_dry":   "--dry-run",
    "claude_screener_watch": "--watch-mode",
    "vb_trader":             "--dry-run",    # vb_trader.py --dry-run 프로세스 식별용
}
```

주의: 기존 항목은 변경하지 않는다. 세 dict 각각에 vb_trader 한 줄씩만 추가한다.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'C:/code/coinbase'); exec(open('C:/code/coinbase/scripts/watchdog.py').read().split('def send_tg')[0]); print('vb_trader in BOTS:', 'vb_trader' in BOTS); print('vb_trader in EXTRA_ARGS:', 'vb_trader' in EXTRA_ARGS); print('claude_screener_dry absent from BOTS:', 'claude_screener_dry' not in BOTS)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "vb_trader" scripts/watchdog.py` 가 3개 이상 출력 (BOTS + EXTRA_ARGS + KILL_KEYWORDS)
    - `grep -n "vb_trader.*vb_trader.py" scripts/watchdog.py` 가 출력 있음 (BOTS 항목)
    - `grep -n "vb_trader.*dry-run" scripts/watchdog.py` 가 출력 있음 (EXTRA_ARGS 항목)
    - `python -c "compile(open('C:/code/coinbase/scripts/watchdog.py').read(),'watchdog.py','exec')"` 가 구문 오류 없음
    - `python -c "content=open('C:/code/coinbase/scripts/watchdog.py',encoding='utf-8').read(); assert 'claude_screener_dry' not in [line.split(':')[0].strip().strip(chr(34)) for line in content.splitlines() if 'claude_screener_dry' in line and 'BOTS[' in content[:content.find('claude_screener_dry')+40]]; print('OK: claude_screener_dry not in BOTS')"` — BOTS dict에 claude_screener_dry 항목 없음 확인 (이미 수동 종료됨)
    - `grep -n "claude_screener_dry" scripts/watchdog.py` 출력에서 BOTS dict 항목이 없음 확인 (EXTRA_ARGS/KILL_KEYWORDS 잔여 항목은 무해)
  </acceptance_criteria>
  <done>watchdog.py BOTS, EXTRA_ARGS, KILL_KEYWORDS 세 dict 모두에 vb_trader 항목이 추가된다. BOTS dict에 claude_screener_dry 항목이 없음도 확인된다.</done>
</task>

<task type="auto">
  <name>Task 2: docs/STRATEGY.md 파라미터 변경 이력 기록</name>
  <files>docs/STRATEGY.md</files>
  <read_first>
    - docs/STRATEGY.md (전체 파일 — 기존 이력 테이블 형식 파악 후 새 행 추가)
  </read_first>
  <action>
CLAUDE.md 기록 규칙 준수: 전략 관련 코드 수정 시 docs/STRATEGY.md의 파라미터 변경 이력 테이블에 추가한다.

docs/STRATEGY.md에서 파라미터 변경 이력 테이블을 찾아 다음 행을 추가한다.
파일에 섹션이나 테이블이 없으면 아래 형식으로 VB 전략 섹션을 새로 작성한다.

추가할 내용 (기존 테이블 마지막 행 뒤에 추가):
```
| 2026-06-06 | vb_trader.py 신규 | K | - | 0.5 | 래리 윌리엄스 VB 전략 표준값 |
| 2026-06-06 | vb_trader.py 신규 | TP | - | +3% | 모의투자 검증용 보수적 목표 |
| 2026-06-06 | vb_trader.py 신규 | SL | - | -2% | alt_monitor OVERSOLD_SL_PCT와 동일 기준 |
| 2026-06-06 | vb_trader.py 신규 | VB_ENTRY_KRW | - | 100,000원 | alt_monitor와 별도 자본, 모의투자 고정 |
| 2026-06-06 | vb_trader.py 신규 | 진입 기준 | - | 당일시가 + 전일범위×0.5 돌파 | /v1/candles/days API 확인 |
| 2026-06-06 | vb_trader.py 신규 | 대상 코인 | - | 24h 거래대금 20억+ | 볼륨 화이트리스트, 자정 갱신 |
| 2026-06-06 | vb_trader.py 신규 | 자정 강제청산 | - | 00:00 KST | 당일 전략 초기화 |
| 2026-06-06 | vb_trader.py 신규 | 실행모드 | - | --dry-run (모의투자) | watchdog EXTRA_ARGS로 자동 적용 |
```

기존 이력 테이블이 없는 경우 아래 전체 블록을 적절한 위치에 삽입:
```markdown
## VB 전략 파라미터 (scripts/vb_trader.py)

*신규 도입: 2026-06-06 — Phase 04 (VB Trader Dry Run)*

| 날짜 | 파일 | 파라미터 | 이전값 | 현재값 | 변경 이유 |
|------|------|---------|--------|--------|----------|
| 2026-06-06 | vb_trader.py 신규 | K (VB 배수) | - | 0.5 | 래리 윌리엄스 VB 전략 표준값 |
| 2026-06-06 | vb_trader.py 신규 | TP | - | +3% | 모의투자 검증용 보수적 목표 |
| 2026-06-06 | vb_trader.py 신규 | SL | - | -2% | alt_monitor OVERSOLD_SL_PCT와 동일 기준 |
| 2026-06-06 | vb_trader.py 신규 | VB_ENTRY_KRW | - | 100,000원 | alt_monitor와 별도 자본, 모의투자 고정 |
| 2026-06-06 | vb_trader.py 신규 | 진입 기준 | - | 당일시가 + 전일범위×0.5 돌파 | /v1/candles/days API 확인 |
| 2026-06-06 | vb_trader.py 신규 | 대상 코인 | - | 24h 거래대금 20억+ | 볼륨 화이트리스트, 자정 갱신 |
| 2026-06-06 | vb_trader.py 신규 | 자정 강제청산 | - | 00:00 KST | 당일 전략 초기화 |
| 2026-06-06 | vb_trader.py 신규 | 실행모드 | - | --dry-run (모의투자) | watchdog EXTRA_ARGS로 자동 적용 |

### 동결 파라미터와의 관계
- vb_trader.py는 alt_monitor.py의 OVERSOLD 전략과 **완전히 별개**다.
- OVERSOLD 파라미터 동결(~2026-06-22)은 vb_trader에 미적용.
- 두 봇은 자본 독립 (vb_trader 고정 10만원 별도 운용).
```
  </action>
  <verify>
    <automated>python -c "content=open('C:/code/coinbase/docs/STRATEGY.md',encoding='utf-8').read(); print('vb_trader' in content and 'K' in content and '0.5' in content)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "vb_trader" docs/STRATEGY.md` 가 5개 이상 출력
    - `grep -n "0.5\|K.*VB\|VB.*K" docs/STRATEGY.md` 가 K=0.5 항목 포함
    - `grep -n "2026-06-06" docs/STRATEGY.md` 가 출력 있음 (날짜 기록 확인)
    - `grep -n "dry-run\|DRY" docs/STRATEGY.md` 가 출력 있음
  </acceptance_criteria>
  <done>
    docs/STRATEGY.md에 VB 전략 파라미터 도입 이력이 기록된다.
    K=0.5, TP=+3%, SL=-2%, 진입금액 10만원, 대상 코인 기준, 자정 강제청산, dry-run 모드가 모두 포함된다.
  </done>
</task>


<task type="auto">
  <name>Task 3: docs/sessions/2026-06-06.md 기록 + git commit (CLAUDE.md 기록 규칙)</name>
  <files>docs/sessions/2026-06-06.md</files>
  <read_first>
    - docs/sessions/2026-06-06.md (존재하면 기존 내용 확인 후 append)
  </read_first>
  <action>
CLAUDE.md 기록 규칙 준수: 전략 관련 코드 수정 시 (1) session 파일 기록, (2) git commit.

**1. docs/sessions/2026-06-06.md 에 Phase 04 작업 내용 append:**

파일이 없으면 새로 생성, 있으면 마지막에 append.

```markdown
## Phase 04: VB Trader Dry Run — 구현 완료

### 변경 내용
- `bithumb/client.py`: `get_daily_candles()` 메서드 추가 (`/v1/candles/days` 엔드포인트)
- `scripts/vb_trader.py`: 신규 생성
  - 단일인스턴스 강제 (포트 47220)
  - VB 전략 상수: K=0.5, TP=+3%, SL=-2%, 진입금액=10만원
  - PriceTracker (WebSocket 실시간 가격)
  - calc_vb_target(): 당일시가 + 전일범위×K
  - run() 메인 루프: 목표가 돌파 감지, 진입/청산, 자정강제청산, 중복방지
  - --dry-run 모드로만 운용 (실제 주문 없음, DB에 [VB-DRY] 태그)
- `scripts/watchdog.py`: BOTS/EXTRA_ARGS/KILL_KEYWORDS에 vb_trader 항목 추가
- `docs/STRATEGY.md`: VB 전략 파라미터 도입 이력 기록

### 파라미터 (동결 없음 — alt_monitor OVERSOLD와 완전 별개)
| 파라미터 | 값 |
|---------|-----|
| K | 0.5 |
| TP | +3% |
| SL | -2% |
| 진입금액 | 100,000원 |
| 볼륨 기준 | 24h 20억+ KRW |
| 자정청산 | 00:00 KST |
```

**2. git commit:**

```bash
git add bithumb/client.py scripts/vb_trader.py scripts/watchdog.py docs/STRATEGY.md docs/sessions/2026-06-06.md
git commit -m "feat(vb-trader): implement VB strategy dry-run trader (Phase 04)"
```

주의: config.yaml 은 절대 포함하지 않는다.
  </action>
  <verify>
    <automated>python -c "import subprocess; result=subprocess.run(['git','log','--oneline','-1'],capture_output=True,text=True,cwd='C:/code/coinbase'); print(result.stdout.strip())"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "open('C:/code/coinbase/docs/sessions/2026-06-06.md',encoding='utf-8').read()"` 가 오류 없음 (파일 존재)
    - `grep -n "vb_trader\|VB Trader" docs/sessions/2026-06-06.md` 가 출력 있음
    - `git log --oneline -1` 에 "vb-trader" 또는 "Phase 04" 관련 커밋 메시지 포함
    - `git status` 에 unstaged/untracked 파일로 bithumb/client.py, scripts/vb_trader.py, watchdog.py, STRATEGY.md 가 없음 (모두 커밋됨)
  </acceptance_criteria>
  <done>
    docs/sessions/2026-06-06.md에 Phase 04 작업 내용이 기록되고, 변경된 모든 파일이 하나의 git commit으로 묶인다.
    CLAUDE.md 기록 규칙 (코드 수정 → 문서 업데이트 → 커밋) 이 준수된다.
  </done>
</task>
</tasks>

<verification>
1. `grep -n "vb_trader" scripts/watchdog.py` — 3개 항목 (BOTS + EXTRA_ARGS + KILL_KEYWORDS) 확인
2. `grep -n "dry-run" scripts/watchdog.py` — vb_trader EXTRA_ARGS 항목 확인
3. `grep -n "vb_trader" docs/STRATEGY.md` — 파라미터 이력 기록 확인
4. `python -c "compile(open('C:/code/coinbase/scripts/watchdog.py').read(),'f','exec')"` — watchdog 구문 오류 없음
5. `grep -c "2026-06-06" docs/STRATEGY.md` — 여러 날짜 행 확인

**Phase 04 전체 통합 검증 (04-01 ~ 04-03 완료 후):**
```bash
# 1. BithumbClient.get_daily_candles 메서드 존재
grep -n "def get_daily_candles" bithumb/client.py

# 2. vb_trader.py 실행 가능 (구문 오류 없음)
python -c "compile(open('scripts/vb_trader.py').read(),'f','exec')" && echo OK

# 3. watchdog 통합 확인
grep "vb_trader" scripts/watchdog.py

# 4. STRATEGY.md 기록 확인
grep -c "vb_trader" docs/STRATEGY.md

# 5. trades DB에 VB-DRY 필터 쿼리 (봇 실행 후 진입 발생 시)
# sqlite3 data/trades.db "SELECT coin, exit_reason, pnl_krw FROM trades WHERE exit_reason LIKE '%VB-DRY%' ORDER BY exited_at DESC LIMIT 10"
```
</verification>

<success_criteria>
- scripts/watchdog.py BOTS에 "vb_trader": ROOT / "scripts" / "vb_trader.py" 추가됨
- scripts/watchdog.py EXTRA_ARGS에 "vb_trader": ["--dry-run"] 추가됨
- scripts/watchdog.py KILL_KEYWORDS에 "vb_trader": "--dry-run" 추가됨
- watchdog 실행 시 vb_trader.py가 --dry-run 인자로 자동 시작되고 장애 후 재시작됨
- docs/STRATEGY.md에 VB 전략 파라미터 도입 이력이 날짜 포함하여 기록됨
- CLAUDE.md 기록 규칙(코드 수정 → STRATEGY.md 업데이트) 준수됨
</success_criteria>

<output>
After completion, create `.planning/phases/04-live-trading/04-03-SUMMARY.md`
</output>

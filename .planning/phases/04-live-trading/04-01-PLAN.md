---
phase: 04-live-trading
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - bithumb/client.py
  - scripts/vb_trader.py
autonomous: true
requirements:
  - VB-01
  - VB-02

must_haves:
  truths:
    - "BithumbClient.get_daily_candles('KRW-BTC', count=2)를 호출하면 오늘 시가와 전일 고저폭을 담은 캔들 2개가 반환된다"
    - "python scripts/vb_trader.py --dry-run을 실행하면 포트 47220을 바인딩하고 시작 로그가 출력된다"
    - "로그에 '볼륨필터 N개 코인 (20억+)' 메시지가 출력된다"
    - "중복 실행 시 '이미 실행 중 (포트 47220)' 메시지와 함께 종료된다"
  artifacts:
    - path: "bithumb/client.py"
      provides: "get_daily_candles() 메서드"
      contains: "def get_daily_candles"
    - path: "scripts/vb_trader.py"
      provides: "봇 골격 — 단일인스턴스, 상수, 로깅, 볼륨 화이트리스트"
      contains: "_ensure_single_instance"
  key_links:
    - from: "scripts/vb_trader.py"
      to: "bithumb/client.py"
      via: "BithumbClient.get_daily_candles()"
      pattern: "get_daily_candles"
    - from: "scripts/vb_trader.py"
      to: "bithumb/client.get_ticker('ALL')"
      via: "_build_volume_whitelist()"
      pattern: "acc_trade_value_24H"
---

<objective>
BithumbClient에 일봉 캔들 메서드를 추가하고, vb_trader.py의 실행 골격(단일 인스턴스 강제, 상수 정의, 로깅 설정, 볼륨 화이트리스트 구축)을 구현한다.

Purpose: 이후 플랜(메인 루프, watchdog 통합)이 의존하는 API 인터페이스와 봇 뼈대를 먼저 확립한다.
Output: bithumb/client.py에 get_daily_candles() 메서드 추가, scripts/vb_trader.py 실행 골격 완성
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-live-trading/04-CONTEXT.md
@.planning/phases/04-live-trading/04-RESEARCH.md

<interfaces>
<!-- 아래 인터페이스를 executor가 직접 참고한다. 별도 코드베이스 탐색 불필요. -->

From bithumb/client.py (현재 마지막 메서드):
```python
BASE_URL = "https://api.bithumb.com"

class BithumbClient:
    def __init__(self, config_path: str = "config.yaml"): ...
    def get_ticker(self, coin: str = "ALL") -> dict: ...
    # get_candles: 분봉만 지원 (unit=1/3/5/10/15/30/60/240)
    def get_candles(self, market: str, unit: int = 5, count: int = 6) -> list[dict]: ...
    # 일봉 없음 — get_daily_candles() 추가 필요
```

추가할 메서드 (bithumb/client.py 끝에 append):
```python
def get_daily_candles(self, market: str, count: int = 2) -> list[dict]:
    """Get daily candles via /v1/candles/days. Returns newest-first.
    idx[0] = today (opening_price = 당일 시가)
    idx[1] = yesterday (high_price, low_price = 전일 고저폭)
    Public endpoint — no auth required."""
    resp = self._session.get(
        f"{BASE_URL}/v1/candles/days",
        params={"market": market.upper(), "count": count},
    )
    resp.raise_for_status()
    return resp.json()
```

From scripts/alt_monitor.py (단일인스턴스 패턴 — port 47219):
```python
_singleton_sock = None
def _ensure_single_instance() -> None:
    import socket as _socket
    global _singleton_sock
    _singleton_sock = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
    _singleton_sock.setsockopt(_socket.SOL_SOCKET, _socket.SO_REUSEADDR, 0)
    try:
        _singleton_sock.bind(("127.0.0.1", 47219))
    except OSError:
        print("[ERROR] alt_monitor 이미 실행 중 (포트 47219 사용 중). 종료합니다.")
        sys.exit(1)
    atexit.register(_singleton_sock.close)
_ensure_single_instance()
```
vb_trader.py는 포트 47220 사용 (충돌 방지).

From scripts/alt_monitor.py (볼륨 화이트리스트 — line ~257-284):
```python
MIN_DAILY_VOLUME_KRW = 20_000_000_000  # 20억 KRW
# get_ticker("ALL") 반환 dict에서:
#   for coin, data in tickers.items():
#       vol = float(data.get("acc_trade_value_24H", 0))
#       if vol >= MIN_DAILY_VOLUME_KRW: wl.add(coin)
```

From scripts/claude_screener.py (--dry-run 파싱 패턴):
```python
def _parse_args() -> argparse.Namespace:
    p = argparse.ArgumentParser(add_help=False)
    p.add_argument("--dry-run", action="store_true")
    p.add_argument("--live",    action="store_true")
    args, _ = p.parse_known_args()
    return args
_args = _parse_args()
_DRY_RUN = _args.dry_run
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BithumbClient.get_daily_candles() 메서드 추가</name>
  <files>bithumb/client.py</files>
  <read_first>
    - bithumb/client.py (전체 파일 — 현재 메서드 목록 파악 후 끝에 추가)
  </read_first>
  <action>
bithumb/client.py의 get_candles() 메서드 바로 다음 줄에 아래 메서드를 추가한다.

```python
def get_daily_candles(self, market: str, count: int = 2) -> list[dict]:
    """Get daily candles via /v1/candles/days. Returns newest-first.

    idx[0] = today  — opening_price: 당일 시가
    idx[1] = yesterday — high_price, low_price: 전일 고저폭 계산용
    Public endpoint, no auth required.
    Anti-pattern: get_candles(unit=1440) 사용 금지 — 빗썸 API 미지원.
    """
    resp = self._session.get(
        f"{BASE_URL}/v1/candles/days",
        params={"market": market.upper(), "count": count},
    )
    resp.raise_for_status()
    return resp.json()
```

주의: `_make_token()` 호출 없음 — Public 엔드포인트이므로 인증 불필요.
`self._session`을 재사용 (기존 get_candles 패턴과 동일).
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'C:/code/coinbase'); from bithumb.client import BithumbClient; print('get_daily_candles' in dir(BithumbClient))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def get_daily_candles" bithumb/client.py` 가 출력 있음
    - `grep -n "v1/candles/days" bithumb/client.py` 가 출력 있음
    - `python -c "from bithumb.client import BithumbClient"` 가 오류 없이 완료
  </acceptance_criteria>
  <done>BithumbClient.get_daily_candles(market, count=2) 메서드가 bithumb/client.py에 존재하고, import 오류 없이 로드된다.</done>
</task>

<task type="auto">
  <name>Task 2: vb_trader.py 골격 — 상수, 로깅, 단일인스턴스, 볼륨 화이트리스트</name>
  <files>scripts/vb_trader.py</files>
  <read_first>
    - scripts/alt_monitor.py (lines 1-70: import 구조, 단일인스턴스 패턴, 로깅 설정)
    - scripts/claude_screener.py (lines 63-78: --dry-run argparse 패턴)
    - bithumb/client.py (전체: get_ticker 시그니처 확인)
  </read_first>
  <action>
scripts/vb_trader.py를 신규 생성한다. 이 플랜에서는 실행 골격만 구현하며, 메인 루프(진입/청산 로직)는 04-02-PLAN.md에서 추가한다.

**파일 구조 (이 플랜 범위):**

```
모듈 docstring
imports (sys, os, atexit, time, json, logging, threading, argparse, socket, yaml, pathlib, datetime, collections.deque)
KST timezone 정의
_ensure_single_instance() — 포트 47220 바인딩
_ensure_single_instance() 즉시 호출
sys.path.insert
bithumb imports
로깅 설정 (logs/vb_trader.log)

# 전략 상수
K = 0.5
VB_TP = 0.03
VB_SL = -0.02
VB_ENTRY_KRW = 100_000
MIN_DAILY_VOLUME_KRW = 20_000_000_000
SCAN_SEC = 2
WS_URL = "wss://pubwss.bithumb.com/pub/ws"
WS_MIN_INTERVAL = 1.0
POS_PATH = Path("data/vb_pos.json")

# --dry-run / --live 파싱
_parse_args() 함수
_args = _parse_args()
_DRY_RUN: bool = _args.dry_run
_LOG_TAG: str = "VB-DRY" if _DRY_RUN else "VB"

# 볼륨 화이트리스트
_build_volume_whitelist(client: BithumbClient) -> set[str]

# 포지션 파일 I/O
load_pos() -> dict | None
save_pos(pos: dict | None) -> None

if __name__ == "__main__": main()
```

**상세 구현 지침:**

1. `_ensure_single_instance()`: alt_monitor.py의 패턴과 동일하되 포트를 **47220**으로 변경.
   오류 메시지: `"[ERROR] vb_trader 이미 실행 중 (포트 47220 사용 중). 종료합니다."`

2. 로깅: format `"%(asctime)s [VB][%(levelname)s] %(message)s"`, handlers: StreamHandler(stdout) + FileHandler("logs/vb_trader.log", encoding="utf-8"), level=INFO.

3. `_build_volume_whitelist(client)` 구현:
   ```python
   def _build_volume_whitelist(client: BithumbClient) -> set[str]:
       """24h 거래대금 20억+ 코인 심볼 반환."""
       try:
           tickers = client.get_ticker("ALL")
           wl: set[str] = set()
           for coin, data in tickers.items():
               if coin == "date":
                   continue
               vol = float(data.get("acc_trade_value_24H", 0))
               if vol >= MIN_DAILY_VOLUME_KRW:
                   wl.add(coin)
           log.info(f"[볼륨필터] {len(wl)}개 코인 (20억+)")
           return wl
       except Exception as e:
           log.warning(f"[볼륨필터] 갱신 실패: {e}")
           return set()
   ```

4. `load_pos()` / `save_pos()`:
   ```python
   def load_pos() -> dict | None:
       if not POS_PATH.exists():
           return None
       try:
           return json.loads(POS_PATH.read_text(encoding="utf-8"))
       except Exception:
           return None

   def save_pos(pos: dict | None) -> None:
       POS_PATH.parent.mkdir(exist_ok=True)
       if pos is None:
           POS_PATH.unlink(missing_ok=True)
       else:
           POS_PATH.write_text(json.dumps(pos, default=str), encoding="utf-8")
   ```

5. `main()` 함수: 시작 로그 출력 후 `run()` 호출. `run()`은 이 플랜에서는 stub `log.info("[VB] run() stub — 04-02에서 구현")` + `while True: time.sleep(60)` 만 작성.

6. `_parse_args()`: argparse `--dry-run`, `--live` 두 플래그만. `add_help=False`, `parse_known_args()` 사용.

7. `if __name__ == "__main__":` 블록:
   ```python
   if __name__ == "__main__":
       from bithumb.db import init_db
       init_db()
       main()
   ```
  </action>
  <verify>
    <automated>python -c "import sys, ast; sys.path.insert(0,'C:/code/coinbase'); tree=ast.parse(open('C:/code/coinbase/scripts/vb_trader.py').read()); names=[n.name for n in ast.walk(tree) if isinstance(n,(ast.ClassDef,ast.FunctionDef))]; checks=['_ensure_single_instance','_build_volume_whitelist','load_pos','save_pos','main']; print('OK' if all(c in names for c in checks) else [c for c in checks if c not in names])"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_ensure_single_instance" scripts/vb_trader.py` 가 출력 있음
    - `grep -n "47220" scripts/vb_trader.py` 가 출력 있음
    - `grep -n "_build_volume_whitelist" scripts/vb_trader.py` 가 출력 있음
    - `grep -n "acc_trade_value_24H" scripts/vb_trader.py` 가 출력 있음
    - `grep -n "VB_TP\|VB_SL\|VB_ENTRY_KRW\|K = 0.5" scripts/vb_trader.py` 가 4개 모두 출력 있음
    - `grep -n "POS_PATH" scripts/vb_trader.py` 가 `data/vb_pos.json` 포함
    - `grep -n "_LOG_TAG" scripts/vb_trader.py` 가 `VB-DRY` 포함
    - `python -c "import sys; sys.path.insert(0,'C:/code/coinbase'); import scripts.vb_trader" 2>&1 | grep -v "이미 실행"` — import 오류 없음 (단, 포트 바인딩으로 두 번째 실행 시 종료 메시지는 정상)
  </acceptance_criteria>
  <done>
    scripts/vb_trader.py가 생성되어 --dry-run 플래그로 실행 가능하다.
    포트 47220 단일 인스턴스, 로깅, 볼륨 화이트리스트, 포지션 I/O, 전략 상수가 모두 포함된다.
    run()은 stub이며 다음 플랜에서 완성된다.
  </done>
</task>

</tasks>

<verification>
1. `grep -n "def get_daily_candles" bithumb/client.py` — 메서드 존재 확인
2. `grep -n "v1/candles/days" bithumb/client.py` — 올바른 엔드포인트 사용 확인
3. `grep -n "47220" scripts/vb_trader.py` — 포트 충돌 없음 확인
4. `grep -n "VB-DRY\|_LOG_TAG" scripts/vb_trader.py` — dry-run 태깅 확인
5. `python -c "from bithumb.client import BithumbClient; c = BithumbClient.__dict__; print('get_daily_candles' in c)"` — 메서드 접근 가능 확인
</verification>

<success_criteria>
- bithumb/client.py에 get_daily_candles() 메서드가 추가됨 (인증 없음, /v1/candles/days 엔드포인트)
- scripts/vb_trader.py가 신규 생성됨 — 포트 47220 단일인스턴스, VB 전략 상수(K=0.5/TP=0.03/SL=-0.02/10만원), 볼륨 화이트리스트 함수, 포지션 I/O 함수 포함
- --dry-run 플래그 파싱 및 [VB-DRY] 태그 설정 포함
- 기존 alt_monitor.py (포트 47219)와 포트 충돌 없음
</success_criteria>

<output>
After completion, create `.planning/phases/04-live-trading/04-01-SUMMARY.md`
</output>

---
phase: 04-live-trading
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - scripts/vb_trader.py
autonomous: true
requirements:
  - VB-02
  - VB-04

must_haves:
  truths:
    - "calc_vb_target()가 KRW-BTC 호출 시 (vb_target, today_open) 튜플을 반환한다"
    - "당일 VB 목표가를 돌파하면 모의 진입 로그와 data/vb_pos.json이 생성된다"
    - "TP(+3%) 또는 SL(-2%) 조건 달성 시 청산 로그가 출력되고 trades DB에 [VB-DRY] 태그로 기록된다"
    - "같은 코인에 당일 중복 진입이 발생하지 않는다 (entered_coins set)"
    - "WebSocket 가격 수신 중단 후 재연결이 자동으로 이루어진다"
  artifacts:
    - path: "scripts/vb_trader.py"
      provides: "PriceTracker, calc_vb_target, _do_sell_dry, _record, run() 메인 루프"
      contains: "def run"
    - path: "data/vb_pos.json"
      provides: "진입 시 자동 생성되는 포지션 파일"
  key_links:
    - from: "scripts/vb_trader.py run()"
      to: "bithumb.db.log_trade()"
      via: "_record() 함수"
      pattern: "log_trade.*VB-DRY"
    - from: "scripts/vb_trader.py PriceTracker"
      to: "wss://pubwss.bithumb.com/pub/ws"
      via: "start_ws(symbols)"
      pattern: "start_ws"
    - from: "scripts/vb_trader.py run()"
      to: "BithumbClient.get_daily_candles()"
      via: "calc_vb_target()"
      pattern: "calc_vb_target"
---

<objective>
vb_trader.py에 메인 루프 전체를 구현한다: PriceTracker(WebSocket 실시간 가격), VB 목표가 계산, 모의 진입/청산 상태 머신, DB 기록.

Purpose: 변동성 돌파 신호 감지 → 모의 진입 → TP/SL 청산의 전체 사이클을 완성한다.
Output: scripts/vb_trader.py의 run() 함수가 완전히 동작하는 상태
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-live-trading/04-CONTEXT.md
@.planning/phases/04-live-trading/04-RESEARCH.md
@.planning/phases/04-live-trading/04-01-SUMMARY.md

<interfaces>
<!-- executor가 직접 참고하는 인터페이스 — 별도 탐색 불필요 -->

From bithumb/db.py — log_trade() 전체 시그니처:
```python
def log_trade(
    coin: str,
    market: str,
    entry_price: float,
    exit_price: float,
    volume: float,           # 코인 수량 (= cost_krw / entry_price)
    cost_krw: float,
    received_krw: float,     # exit_price * volume
    exit_reason: str,        # "[VB-DRY] TP+3%" 형식
    entered_at: datetime,
    exited_at: datetime,
    max_price: float = 0.0,
    entry_chg24h: float | None = None,
    claude_reason: str | None = None,
    entry_chg5m: float | None = None,
    entry_btc_chg: float | None = None,
    entry_n_pos: int | None = None,
) -> None: ...
```

From bithumb/notify.py:
```python
def send(text: str, force: bool = False) -> bool:
    """Telegram 알림. 무음 시간대엔 전송 안 함 (force=True면 강제)."""
```

From scripts/claude_screener.py (_record 패턴 — VB용으로 수정):
```python
def _record(pos: dict, exit_price: float, recv_krw: float, reason: str) -> None:
    try:
        log_trade(
            coin=pos["coin"], market=pos["market"],
            entry_price=pos["entry_price"], exit_price=exit_price,
            volume=pos["volume"], cost_krw=pos["cost_krw"],
            received_krw=recv_krw,
            exit_reason=f"[{_LOG_TAG}] {reason}",
            entered_at=datetime.fromisoformat(pos["entered_at"]).replace(tzinfo=None),
            exited_at=datetime.now(),
            max_price=pos.get("highest", exit_price),
        )
    except Exception as e:
        log.error(f"[DB] 기록 실패: {e}")
```

VB 포지션 dict 구조 (data/vb_pos.json):
```json
{
  "coin": "BTC",
  "market": "KRW-BTC",
  "entry_price": 94502500.0,
  "volume": 0.001057,
  "cost_krw": 100000,
  "highest": 95000000.0,
  "entered_at": "2026-06-06T10:30:00",
  "vb_target": 94502500.0,
  "today_open": 92252000.0,
  "mock": true
}
```

PriceTracker (alt_monitor.py:713-784에서 추출):
- `start_ws(symbols: list[str])` — symbols 형식: ["BTC_KRW", "ETH_KRW", ...]
- `get_latest_price(coin: str) -> float` — 0.0이면 미수신
- `stop_ws()` — 종료 시 호출
- WS_URL = "wss://pubwss.bithumb.com/pub/ws"
- WS_MIN_INTERVAL = 1.0 (초)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PriceTracker 클래스 + calc_vb_target() + _record()/_do_sell_dry() 함수 추가</name>
  <files>scripts/vb_trader.py</files>
  <read_first>
    - scripts/vb_trader.py (전체 — 04-01 결과 파악, 삽입 위치 확인)
    - scripts/alt_monitor.py (lines 710-800: PriceTracker 전체 구현)
    - scripts/claude_screener.py (lines 470-540: _do_sell_dry, _record 패턴)
  </read_first>
  <action>
04-01에서 생성된 vb_trader.py에 아래 세 컴포넌트를 추가한다. run() stub 위에 삽입한다.

**1. PriceTracker 클래스 (alt_monitor.py:713-784 패턴, VB용 단순화)**

alt_monitor.py의 PriceTracker에서 필요한 메서드만 남긴다:
- `__init__`, `start_ws(symbols)`, `stop_ws()`, `get_latest_price(coin) -> float`
- `get_signal()`, `get_preemptive_signal()`, `get_vol_power()`, `get_latest_acc_value()`, `get_latest_exchange_ts()` 는 불필요 — 제외

`get_latest_price(coin)` 구현:
```python
def get_latest_price(self, coin: str) -> float:
    with self._lock:
        hist = self._hist.get(coin)
        if not hist:
            return 0.0
        return hist[-1][1]  # (recv_ts, price, acc_val) 튜플의 인덱스 1
```

`on_message` 내부: hist 튜플은 `(now, price, acc_val)` 3원소 유지 (WS_MIN_INTERVAL 필터 포함).
`start_ws` 스레드: `threading.Thread(target=run, daemon=True).start()`

**2. calc_vb_target() 함수**

```python
def calc_vb_target(client: BithumbClient, coin: str) -> tuple[float, float] | None:
    """(vb_target, today_open) 반환. 실패 시 None.
    vb_target = today_open + (prev_high - prev_low) * K
    """
    try:
        candles = client.get_daily_candles(f"KRW-{coin}", count=2)
        if len(candles) < 2:
            log.warning(f"[{coin}] 일봉 캔들 부족: {len(candles)}개")
            return None
        today_open  = float(candles[0]["opening_price"])
        prev_high   = float(candles[1]["high_price"])
        prev_low    = float(candles[1]["low_price"])
        prev_range  = prev_high - prev_low
        if prev_range <= 0 or today_open <= 0:
            return None
        vb_target = today_open + prev_range * K
        log.debug(f"[{coin}] 시가={today_open:,.0f} 전일범위={prev_range:,.0f} VB목표={vb_target:,.0f}")
        return vb_target, today_open
    except Exception as e:
        log.warning(f"[{coin}] VB 목표가 계산 실패: {e}")
        return None
```

**3. _record() 와 _do_sell_dry() 함수**

`_record()`: DB log_trade 호출. `exit_reason=f"[{_LOG_TAG}] {reason}"` 형식.
`entered_at`은 `datetime.fromisoformat(pos["entered_at"]).replace(tzinfo=None)`.

`_do_sell_dry()`:
```python
def _do_sell_dry(pos: dict, current_price: float, reason: str) -> None:
    """모의 청산: 실제 주문 없이 현재가로 PnL 계산 후 DB 기록."""
    vol     = pos["volume"]
    recv    = current_price * vol
    pnl_krw = recv - pos["cost_krw"]
    pnl_pct = pnl_krw / pos["cost_krw"] * 100
    log.warning(
        f"[{pos['coin']}] [{_LOG_TAG}] 청산 @{current_price:,.0f}원 "
        f"PnL={pnl_pct:+.2f}% ({pnl_krw:+,.0f}원) | {reason}"
    )
    _record(pos, current_price, recv, reason)
    notify.send(
        f"[{_LOG_TAG}] {pos['coin']} 청산 @{current_price:,.0f}원 "
        f"PnL={pnl_pct:+.2f}% | {reason}"
    )
```

`_do_sell_live()` 는 이 플랜 스코프 외 — stub 만 작성:
```python
def _do_sell_live(pos: dict, client: BithumbClient, reason: str) -> None:
    """실거래 청산 — 추후 구현."""
    log.error("[LIVE] 실거래 청산 미구현 — --dry-run 모드만 사용 가능")
```
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'C:/code/coinbase'); import ast; tree=ast.parse(open('C:/code/coinbase/scripts/vb_trader.py').read()); names=[n.name for n in ast.walk(tree) if isinstance(n,(ast.ClassDef,ast.FunctionDef))]; print(all(x in names for x in ['PriceTracker','calc_vb_target','_record','_do_sell_dry']))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class PriceTracker" scripts/vb_trader.py` 가 출력 있음
    - `grep -n "def calc_vb_target" scripts/vb_trader.py` 가 출력 있음
    - `grep -n "def _record" scripts/vb_trader.py` 가 출력 있음
    - `grep -n "def _do_sell_dry" scripts/vb_trader.py` 가 출력 있음
    - `grep -n "get_daily_candles" scripts/vb_trader.py` 가 출력 있음
    - `grep -n "opening_price.*high_price.*low_price\|prev_range.*K" scripts/vb_trader.py` 가 VB 공식 포함 확인
    - `grep -n "_LOG_TAG" scripts/vb_trader.py` 에서 _record 또는 exit_reason 라인에 포함됨
  </acceptance_criteria>
  <done>
    PriceTracker, calc_vb_target(), _record(), _do_sell_dry()가 vb_trader.py에 구현된다.
    PriceTracker는 start_ws/stop_ws/get_latest_price만 포함한 단순화 버전이다.
  </done>
</task>

<task type="auto">
  <name>Task 2: run() 메인 루프 완성 — VB 목표가 계산, 돌파 감지, 진입/청산 상태 머신</name>
  <files>scripts/vb_trader.py</files>
  <read_first>
    - scripts/vb_trader.py (전체 — Task 1 결과 확인 후 run() stub 교체)
    - .planning/phases/04-live-trading/04-RESEARCH.md (lines 415-503: 메인 루프 구조 참고)
  </read_first>
  <action>
Task 1의 stub `run()` 함수를 아래 완전한 구현으로 교체한다.

```python
def run() -> None:
    client  = BithumbClient()
    tracker = PriceTracker()

    whitelist: set[str]   = set()
    vb_targets: dict[str, float] = {}   # coin -> vb_target
    entered_coins: set[str] = set()      # 당일 진입 완료 코인 (중복 방지, 자정 초기화)

    pos: dict | None = load_pos()
    today            = date.today()
    midnight_cleared = False

    # 초기화: 화이트리스트 + VB 목표가 + WebSocket
    whitelist = _build_volume_whitelist(client)
    for coin in whitelist:
        result = calc_vb_target(client, coin)
        if result:
            vb_targets[coin] = result[0]
    log.info(f"[VB] VB 목표가 계산 완료: {len(vb_targets)}개 코인")

    symbols = [f"{c}_KRW" for c in whitelist]
    if symbols:
        tracker.start_ws(symbols)
        time.sleep(5)   # WS 연결 안정화 대기
    else:
        log.warning("[VB] 화이트리스트가 비어 있음 — WebSocket 구독 없음")

    log.info(
        f"[VB] 시작 완료 | 모드={'DRY-RUN' if _DRY_RUN else 'LIVE'} "
        f"| 코인={len(whitelist)}개 | 포지션={'있음: '+pos['coin'] if pos else '없음'}"
    )

    while True:
        try:
            now_kst = datetime.now(KST)

            # 날짜 교체: VB 목표가 + 화이트리스트 재계산
            if date.today() != today:
                today = date.today()
                midnight_cleared = False
                entered_coins.clear()
                log.info("[VB] 날짜 교체 — 화이트리스트/VB목표가 재계산")
                whitelist = _build_volume_whitelist(client)
                vb_targets.clear()
                for coin in whitelist:
                    result = calc_vb_target(client, coin)
                    if result:
                        vb_targets[coin] = result[0]
                # WebSocket 재구독 (코인 목록 변경 반영)
                tracker.stop_ws()
                symbols = [f"{c}_KRW" for c in whitelist]
                if symbols:
                    tracker.start_ws(symbols)
                    time.sleep(5)

            # 자정 강제 청산 (00:00~00:01 KST, 1회만)
            if now_kst.hour == 0 and now_kst.minute == 0 and not midnight_cleared:
                if pos is not None:
                    current = tracker.get_latest_price(pos["coin"])
                    if current > 0:
                        if _DRY_RUN:
                            _do_sell_dry(pos, current, "자정강제청산")
                        else:
                            _do_sell_live(pos, client, "자정강제청산")
                    else:
                        log.warning(f"[{pos['coin']}] 자정강제청산: 가격 미수신, 0원으로 기록")
                        _do_sell_dry(pos, pos["entry_price"], "자정강제청산(가격미수신)")
                    pos = None
                    save_pos(None)
                midnight_cleared = True
                log.info("[VB] 자정 처리 완료 — midnight_cleared=True")

            # 포지션 없음: VB 목표가 돌파 감지
            if pos is None:
                for coin, target in vb_targets.items():
                    if coin in entered_coins:
                        continue  # 당일 이미 진입한 코인 스킵
                    current = tracker.get_latest_price(coin)
                    if current <= 0 or current < target:
                        continue
                    # 목표가 상향 돌파 → 모의 진입
                    volume = VB_ENTRY_KRW / current
                    pos = {
                        "coin":        coin,
                        "market":      f"KRW-{coin}",
                        "entry_price": current,
                        "volume":      volume,
                        "cost_krw":    VB_ENTRY_KRW,
                        "highest":     current,
                        "entered_at":  datetime.now().isoformat(),
                        "vb_target":   target,
                        "mock":        _DRY_RUN,
                    }
                    save_pos(pos)
                    entered_coins.add(coin)
                    log.warning(
                        f"[{coin}] [{_LOG_TAG}] 목표가 돌파! "
                        f"목표={target:,.0f} 현재={current:,.0f}원 → 모의 진입"
                    )
                    notify.send(
                        f"[{_LOG_TAG}] {coin} 모의 진입 "
                        f"@{current:,.0f}원 (목표={target:,.0f}원)"
                    )
                    break  # 1코인 1포지션

            # 포지션 있음: TP/SL 체크
            elif pos is not None:
                coin    = pos["coin"]
                entry   = pos["entry_price"]
                current = tracker.get_latest_price(coin)
                if current <= 0:
                    time.sleep(SCAN_SEC)
                    continue

                if current > pos.get("highest", entry):
                    pos["highest"] = current
                    save_pos(pos)

                pnl_pct = (current - entry) / entry

                if pnl_pct >= VB_TP:
                    reason = f"TP+3% ({pnl_pct * 100:+.1f}%)"
                    if _DRY_RUN:
                        _do_sell_dry(pos, current, reason)
                    else:
                        _do_sell_live(pos, client, reason)
                    pos = None
                    save_pos(None)

                elif pnl_pct <= VB_SL:
                    reason = f"SL-2% ({pnl_pct * 100:+.1f}%)"
                    if _DRY_RUN:
                        _do_sell_dry(pos, current, reason)
                    else:
                        _do_sell_live(pos, client, reason)
                    pos = None
                    save_pos(None)

        except KeyboardInterrupt:
            log.info("[VB] 종료 요청 (Ctrl+C)")
            tracker.stop_ws()
            break
        except Exception as e:
            log.error(f"[VB] 루프 오류: {e}", exc_info=True)

        time.sleep(SCAN_SEC)
```

그리고 `main()` 함수에서 run()을 호출하도록 stub를 교체한다:
```python
def main() -> None:
    log.info(
        f"[VB] vb_trader 시작 — 모드={'DRY-RUN' if _DRY_RUN else 'LIVE'} "
        f"| K={K} | TP={VB_TP*100:.0f}% | SL={abs(VB_SL)*100:.0f}% "
        f"| 진입={VB_ENTRY_KRW:,.0f}원"
    )
    run()
```
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'C:/code/coinbase'); import ast; src=open('C:/code/coinbase/scripts/vb_trader.py').read(); tree=ast.parse(src); fns=[n.name for n in ast.walk(tree) if isinstance(n,ast.FunctionDef)]; checks=['run','main','_do_sell_dry','calc_vb_target','_record']; print('OK' if all(f in fns for f in checks) else [f for f in checks if f not in fns])"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def run" scripts/vb_trader.py` 가 출력 있음
    - `grep -n "entered_coins" scripts/vb_trader.py` 가 2개 이상 (선언 + 사용)
    - `grep -n "midnight_cleared" scripts/vb_trader.py` 가 2개 이상 (자정 처리)
    - `grep -n "VB_TP\|VB_SL" scripts/vb_trader.py` 에서 run() 내 조건 비교 포함
    - `grep -n "save_pos(None)" scripts/vb_trader.py` 가 청산 후 포지션 초기화 확인 (3개: TP/SL/자정)
    - `grep -n "entered_coins.add\|entered_coins.clear" scripts/vb_trader.py` 가 출력 있음
    - `grep -n "1코인 1포지션\|break" scripts/vb_trader.py` 가 진입 후 break 포함
    - `python -c "import sys; sys.path.insert(0,'C:/code/coinbase'); compile(open('C:/code/coinbase/scripts/vb_trader.py').read(),'vb_trader.py','exec')"` 가 구문 오류 없음
  </acceptance_criteria>
  <done>
    run() 메인 루프가 완전히 구현된다.
    날짜 교체 시 VB 목표가 재계산, 자정 강제 청산, 목표가 돌파 감지, TP/SL 청산, entered_coins로 당일 중복 방지가 모두 포함된다.
    --dry-run 모드에서는 _do_sell_dry(), --live 모드에서는 _do_sell_live() 분기가 작동한다.
  </done>
</task>

</tasks>

<verification>
1. `python -c "compile(open('C:/code/coinbase/scripts/vb_trader.py').read(),'f','exec')"` — 구문 오류 없음
2. `grep -c "def " scripts/vb_trader.py` — 함수 8개 이상 (run, main, _build_volume_whitelist, load_pos, save_pos, _record, _do_sell_dry, calc_vb_target, PriceTracker 메서드 포함)
3. `grep -n "VB-DRY\|_LOG_TAG" scripts/vb_trader.py` — exit_reason 태깅 확인
4. `grep -n "entered_coins" scripts/vb_trader.py` — 중복 진입 방지 로직 확인
5. `grep -n "midnight_cleared" scripts/vb_trader.py` — 자정 강제 청산 1회 보장 확인
</verification>

<success_criteria>
- scripts/vb_trader.py에 PriceTracker (start_ws/stop_ws/get_latest_price), calc_vb_target(), _record(), _do_sell_dry(), run() 메인 루프가 모두 구현됨
- run() 루프는 날짜 교체 감지, 자정 강제 청산(midnight_cleared 플래그), 목표가 돌파 진입, TP+3%/SL-2% 청산을 포함함
- entered_coins set으로 당일 코인 중복 진입 방지
- dry-run 모드에서 실제 API 주문 호출 없이 _do_sell_dry()만 호출
- DB 기록은 exit_reason에 [VB-DRY] 태그 포함
</success_criteria>

<output>
After completion, create `.planning/phases/04-live-trading/04-02-SUMMARY.md`
</output>

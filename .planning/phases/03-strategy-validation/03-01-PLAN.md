---
phase: 03-strategy-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/backtest.py
autonomous: true
requirements: [VAL-02]
must_haves:
  truths:
    - "그리드 서치가 조합마다 다른 진입/TP/SL 값으로 simulate_event를 호출할 수 있다"
    - "리팩터 후에도 python scripts/backtest.py 단독 실행이 Phase 2와 동일하게 작동한다"
    - "python scripts/backtest.py --self-test 가 리팩터 후에도 통과한다"
  artifacts:
    - path: "scripts/backtest.py"
      provides: "Strategy 데이터클래스 + 파라미터 주입형 simulate_event"
      contains: "class Strategy"
  key_links:
    - from: "simulate_event"
      to: "Strategy"
      via: "strategy 인자 (기본값 DEFAULT_STRATEGY)"
      pattern: "simulate_event.*strategy.*Strategy"
---

<objective>
Phase 2 백테스트 엔진(`scripts/backtest.py`)의 모듈 레벨 전략 상수
(`ENTRY_DROP_PCT` / `TP_PCT` / `SL_PCT`)를 함수 인자로 주입 가능한
`Strategy` 데이터클래스로 끌어올린다. 이것이 Phase 3 그리드 서치의
핵심 통합 지점이다 — 서치는 조합마다 다른 파라미터로 `simulate_event`를
호출해야 하므로 파라미터화가 불가피하다.

Purpose: VAL-02(그리드 서치)의 기반 마련. 엔진을 재구현하지 않고
재사용 가능한 라이브러리로 전환한다.
Output: 리팩터된 `scripts/backtest.py` — Phase 2 동작·시그니처 비파괴.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strategy-validation/03-CONTEXT.md
@.planning/phases/03-strategy-validation/03-RESEARCH.md
@scripts/backtest.py

<interfaces>
<!-- backtest.py 현재 전략 상수 (모듈 레벨, L13-19) — 이번에 파라미터화 대상 -->
ENTRY_DROP_PCT = 0.07   # 진입: 주행 고점 대비 -7% 눌림
TP_PCT         = 0.05   # 익절 +5%
SL_PCT         = 0.03   # 손절 -3%
TIMEOUT_SEC    = 600    # D-09: 고정 — 탐색 대상 아님, 모듈 상수 유지
MIN_TICKS      = 4      # 탐색 대상 아님 — 모듈 상수 유지
GAP_EXCLUDE_PCT = 0.30  # 탐색 대상 아님 — 모듈 상수 유지

<!-- 현재 시그니처 (L122) — strategy 인자 추가, 기본값으로 하위호환 -->
def simulate_event(ticks: list[dict], slippage: float) -> dict | None

<!-- print_report 가 전략 상수를 직접 참조하는 위치: L336-338 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strategy 데이터클래스 정의 + simulate_event 파라미터화</name>
  <files>scripts/backtest.py</files>
  <read_first>
    - scripts/backtest.py (전체 — 특히 L13-19 전략 상수, L122-170 simulate_event)
    - .planning/phases/03-strategy-validation/03-RESEARCH.md (§"Pattern 1: 전략 상수를 파라미터로 끌어올리기")
    - .planning/phases/03-strategy-validation/03-CONTEXT.md (D-09)
  </read_first>
  <behavior>
    - Strategy(0.05, 0.07, 0.03) 생성 시 entry_drop_pct=0.05, tp_pct=0.07, sl_pct=0.03
    - Strategy() 인자 없이 생성 시 DEFAULT_STRATEGY와 동일 (0.07/0.05/0.03)
    - Strategy는 frozen — 필드 재할당 시 FrozenInstanceError
    - simulate_event(ticks, slip) 인자 2개 호출 시 DEFAULT_STRATEGY 적용 (Phase 2 동작)
    - simulate_event(ticks, slip, Strategy(0.10, 0.03, 0.05)) 호출 시 진입 -10%/TP +3%/SL -5% 적용
  </behavior>
  <action>
    `scripts/backtest.py` 상단(import 직후)에 frozen 데이터클래스를 추가한다:

    ```python
    from dataclasses import dataclass

    @dataclass(frozen=True)
    class Strategy:
        """그리드 서치가 주입하는 전략 파라미터 묶음. TIMEOUT_SEC은 D-09에 따라 고정."""
        entry_drop_pct: float = 0.07
        tp_pct: float = 0.05
        sl_pct: float = 0.03

    DEFAULT_STRATEGY = Strategy()
    ```

    기존 모듈 상수 `ENTRY_DROP_PCT` / `TP_PCT` / `SL_PCT`(L14-16)는 **삭제하고**
    `DEFAULT_STRATEGY` 필드로 일원화한다. `TIMEOUT_SEC` / `MIN_TICKS` /
    `GAP_EXCLUDE_PCT` / `ROUND_TRIP_FEE` / `SLIPPAGE_SCENARIOS` / `Z_95` /
    `MIN_SAMPLE`은 탐색 대상이 아니므로 모듈 상수로 그대로 둔다 (D-09).

    `simulate_event` 시그니처를 변경한다:
    `def simulate_event(ticks: list[dict], slippage: float, strategy: Strategy = DEFAULT_STRATEGY) -> dict | None:`

    본문에서 전역 상수 참조를 strategy 필드 참조로 치환한다:
    - L145 `if drawdown <= -ENTRY_DROP_PCT:` → `if drawdown <= -strategy.entry_drop_pct:`
    - L161 `hit = "익절" if chg >= TP_PCT else ("손절" if chg <= -SL_PCT else None)`
      → `hit = "익절" if chg >= strategy.tp_pct else ("손절" if chg <= -strategy.sl_pct else None)`

    `_close()`는 전략 파라미터를 쓰지 않으므로 변경 불필요.
    `run_backtest`의 `simulate_event` 호출(L308)은 인자 2개 그대로 둔다 —
    기본값 `DEFAULT_STRATEGY`가 적용돼 Phase 2 동작이 보존된다.
  </action>
  <verify>
    <automated>python scripts/backtest.py --self-test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class Strategy" scripts/backtest.py` 가 1건 매치
    - `grep -n "DEFAULT_STRATEGY = Strategy()" scripts/backtest.py` 가 1건 매치
    - `grep -nE "^ENTRY_DROP_PCT|^TP_PCT|^SL_PCT" scripts/backtest.py` 가 0건 (모듈 상수 삭제됨)
    - `grep -n "strategy: Strategy = DEFAULT_STRATEGY" scripts/backtest.py` 가 1건 매치
    - `grep -n "strategy.entry_drop_pct" scripts/backtest.py` / `strategy.tp_pct` / `strategy.sl_pct` 각 1건 이상
    - `python scripts/backtest.py --self-test` 출력에 "self-test PASS" 포함, exit code 0
  </acceptance_criteria>
  <done>Strategy 데이터클래스 존재, simulate_event가 strategy 인자를 받으며 기본값으로 하위호환, self-test 통과</done>
</task>

<task type="auto">
  <name>Task 2: print_report 전략 상수 참조 수정 + Phase 2 회귀 확인</name>
  <files>scripts/backtest.py</files>
  <read_first>
    - scripts/backtest.py (L324-365 print_report — Task 1 수정 후 상태)
    - .planning/phases/03-strategy-validation/03-RESEARCH.md (§"Pitfall 1: 전략 상수 파라미터화가 Phase 2를 깨뜨림")
  </read_first>
  <action>
    `print_report`의 L336-338가 삭제된 모듈 상수 `ENTRY_DROP_PCT` /
    `TP_PCT` / `SL_PCT`를 직접 참조해 NameError가 난다. 이를
    `DEFAULT_STRATEGY` 필드 참조로 치환한다:

    ```python
    print(f"전략 상수: 진입 -{DEFAULT_STRATEGY.entry_drop_pct * 100:.0f}%  "
          f"TP +{DEFAULT_STRATEGY.tp_pct * 100:.0f}%  SL -{DEFAULT_STRATEGY.sl_pct * 100:.0f}%  "
          f"시간초과 {TIMEOUT_SEC}초")
    ```

    그 후 `backtest.py` 전체에서 `ENTRY_DROP_PCT` / `TP_PCT` / `SL_PCT`
    잔여 참조를 grep으로 찾아 모두 제거됐는지 확인한다.

    Phase 2 회귀 확인: `python scripts/backtest.py` 를 단독 실행한다.
    `pump_ticks`가 0행이므로 "백테스트 대상 이벤트 없음 — 틱 데이터
    축적 필요" 메시지가 출력되고 exit code 0이어야 한다. NameError나
    TypeError가 발생하면 실패다.
  </action>
  <verify>
    <automated>python scripts/backtest.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "[^.]ENTRY_DROP_PCT|[^.]TP_PCT[^_]|[^.]SL_PCT[^_]" scripts/backtest.py` 가 모듈 상수 형태로는 0건 (strategy.tp_pct 같은 필드 접근만 남음)
    - `grep -n "DEFAULT_STRATEGY.entry_drop_pct" scripts/backtest.py` 가 1건 이상
    - `python scripts/backtest.py` 가 exit code 0, NameError/TypeError 미발생
    - `python scripts/backtest.py --self-test` 가 여전히 "self-test PASS" 출력
  </acceptance_criteria>
  <done>print_report가 DEFAULT_STRATEGY를 참조, backtest.py 단독 실행이 Phase 2와 동일하게 작동 (회귀 없음)</done>
</task>

</tasks>

<verification>
- `python scripts/backtest.py --self-test` → "self-test PASS", exit 0
- `python scripts/backtest.py` → 단독 실행 성공 (이벤트 없음 메시지), exit 0
- `python -c "from scripts.backtest import Strategy, simulate_event, DEFAULT_STRATEGY"` (또는 sys.path 조정 후) import 성공
- Strategy가 frozen — 필드 재할당 시 예외
</verification>

<success_criteria>
- backtest.py가 import 가능한 라이브러리로 전환됨 (Strategy 주입 가능)
- Phase 2 단독 실행·self-test 동작이 한 글자도 깨지지 않음
- 그리드 서치가 simulate_event(ticks, slip, Strategy(...))로 임의 조합 호출 가능
</success_criteria>

<output>
After completion, create `.planning/phases/03-strategy-validation/03-01-SUMMARY.md`
</output>

---
phase: 01-tick-recording-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bithumb/db.py
autonomous: true
requirements: [REC-02, REC-03, REC-04]
must_haves:
  truths:
    - "init_db() 실행 후 SQLite에 pump_ticks 테이블이 존재한다"
    - "log_tick()을 호출하면 pump_ticks에 행이 INSERT되고 SELECT COUNT(*)로 확인된다"
    - "각 틱 행에 exchange_ts와 recv_ts 컬럼이 분리 저장된다"
    - "exchange_ts가 None이면 recv_ts 값이 복사되고 ts_estimated=1로 기록된다"
    - "get_ticks(pump_id)가 해당 이벤트의 모든 틱을 seq 순으로 반환한다"
  artifacts:
    - path: "bithumb/db.py"
      provides: "pump_ticks 테이블 스키마 + log_tick/get_ticks 함수"
      contains: "CREATE TABLE IF NOT EXISTS pump_ticks"
  key_links:
    - from: "bithumb/db.py log_tick()"
      to: "pump_ticks 테이블"
      via: "INSERT INTO pump_ticks"
      pattern: "INSERT INTO pump_ticks"
    - from: "bithumb/db.py get_ticks()"
      to: "pump_ticks 테이블"
      via: "SELECT ORDER BY seq"
      pattern: "SELECT \\* FROM pump_ticks"
---

<objective>
펌핑 이벤트의 초 단위 가격 틱을 영속화할 `pump_ticks` SQLite 테이블과 쓰기/읽기 함수(`log_tick` / `get_ticks`)를 `bithumb/db.py`에 추가한다.

Purpose: Phase 1의 데이터 저장 계층 — 틱이 버려지지 않고 DB에 쌓이게 한다. Phase 2 백테스트 엔진이 이 스키마와 함수에 직접 의존하므로 시그니처를 이 plan에서 동결한다.
Output: `pump_ticks` 테이블 스키마(`CREATE_SQL` 추가), `log_tick()` / `get_ticks()` 모듈 레벨 함수. `pump_log`와 기존 코드는 일절 건드리지 않는다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tick-recording-infrastructure/01-CONTEXT.md
@.planning/phases/01-tick-recording-infrastructure/01-RESEARCH.md

<interfaces>
<!-- bithumb/db.py 기존 패턴 (실행자는 이 패턴을 그대로 따를 것, 코드베이스 탐색 불필요) -->

bithumb/db.py 의 기존 구조:
- L14~78: `CREATE_SQL` 문자열 — 모든 CREATE TABLE 문을 담은 단일 문자열. 끝(L77 `);` 뒤)에 pump_ticks 정의를 추가한다.
- L81~85: `_conn()` — `DB_PATH.parent.mkdir(exist_ok=True)` 후 `sqlite3.connect`, `con.row_factory = sqlite3.Row`.
- L88~102: `init_db()` — `con.executescript(CREATE_SQL)` 실행 후, `(tbl, col, typ)` 리스트를 순회하며 `ALTER TABLE ADD COLUMN`을 try/except로 감싸 멱등 마이그레이션.
- L168: `log_pump(coin, detected_at, base_price, ...)` — `with _conn() as con: con.execute("INSERT INTO ...", (...))` 패턴, 모듈 레벨 함수, 클래스 없음.
- L217: `get_trades(days)` — `with _conn() as con: rows = con.execute("SELECT ...").fetchall(); return [dict(r) for r in rows]` 패턴.

기존 pump_log 스키마 (L60~77) — 절대 수정 금지, FK 참조 대상:
```sql
CREATE TABLE IF NOT EXISTS pump_log (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    detected_at   TEXT    NOT NULL,
    coin          TEXT    NOT NULL,
    base_price    REAL    NOT NULL,
    ...
);
```
`pump_id`로 참조할 부모 키는 `pump_log.id`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: pump_ticks 테이블 스키마를 CREATE_SQL에 추가</name>
  <files>bithumb/db.py</files>
  <read_first>
    - bithumb/db.py (전체 — 특히 L14~102: CREATE_SQL 문자열, _conn(), init_db())
    - .planning/phases/01-tick-recording-infrastructure/01-RESEARCH.md ("Pattern 2: SQLite 시계열 자식 테이블" 섹션 L135~156)
  </read_first>
  <action>
    `bithumb/db.py`의 `CREATE_SQL` 문자열(L14~78) 안, `pump_log` CREATE TABLE 문 닫는 `);` 바로 뒤(L77~78 사이)에 아래 SQL을 정확히 추가한다. `"""` 닫는 따옴표 앞에 위치해야 한다:

    ```sql
    CREATE TABLE IF NOT EXISTS pump_ticks (
        id            INTEGER PRIMARY KEY AUTOINCREMENT,
        pump_id       INTEGER NOT NULL,        -- pump_log.id 참조 (논리적 FK)
        seq           INTEGER NOT NULL,        -- 이벤트 내 절대 순번 (0,1,2,...)
        exchange_ts   REAL,                    -- 거래소 발생 시각 (epoch sec)
        recv_ts       REAL    NOT NULL,        -- 수집기 수신 시각 (time.time())
        price         REAL    NOT NULL,        -- closePrice
        acc_value     REAL,                    -- 누적 거래대금 (value)
        volume_power  REAL,                    -- 체결강도 (volumePower)
        gap_before    INTEGER DEFAULT 0,       -- 직전 틱과 갭이면 1 (REC-04)
        ts_estimated  INTEGER DEFAULT 0        -- exchange_ts가 recv_ts 복사값이면 1 (REC-03)
    );
    CREATE INDEX IF NOT EXISTS idx_pump_ticks_pump_id ON pump_ticks(pump_id);
    ```

    설계 결정 (Claude's Discretion 확정):
    - 모든 timestamp는 `REAL`(epoch 초). `seq`/플래그는 `INTEGER`. 가격/거래대금은 `REAL`.
    - `seq`는 절대 순번(0,1,2,...) — 시간 공백은 `recv_ts`/`exchange_ts`와 `gap_before`로 표현 (RESEARCH Pitfall 5 권장).
    - SQLite는 FOREIGN KEY를 기본 미강제하므로 `pump_id`는 논리적 참조 + `idx_pump_ticks_pump_id` 인덱스로 JOIN 성능만 확보. `PRAGMA foreign_keys` 추가하지 않는다 (기존 코드 패턴 일치).

    `init_db()`의 `(tbl, col, typ)` ALTER TABLE 리스트(L91~97)는 **변경하지 않는다** — `pump_ticks`는 신규 테이블이라 `CREATE TABLE IF NOT EXISTS`로 자동 생성된다. `pump_log`나 기존 CREATE 문은 한 줄도 수정하지 않는다.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'.'); from bithumb.db import init_db, _conn; init_db(); con=_conn(); cols=[r[1] for r in con.execute('PRAGMA table_info(pump_ticks)')]; assert cols==['id','pump_id','seq','exchange_ts','recv_ts','price','acc_value','volume_power','gap_before','ts_estimated'], cols; idx=[r[1] for r in con.execute('PRAGMA index_list(pump_ticks)')]; assert 'idx_pump_ticks_pump_id' in idx, idx; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE IF NOT EXISTS pump_ticks" bithumb/db.py` 가 1을 반환
    - `grep -c "idx_pump_ticks_pump_id" bithumb/db.py` 가 2 이상 (CREATE INDEX + 향후 참조)
    - `PRAGMA table_info(pump_ticks)` 컬럼 순서가 정확히: id, pump_id, seq, exchange_ts, recv_ts, price, acc_value, volume_power, gap_before, ts_estimated
    - `grep -c "CREATE TABLE IF NOT EXISTS pump_log" bithumb/db.py` 가 여전히 1 (pump_log 미변경)
    - init_db() 호출 후 위 verify 스크립트가 "OK" 출력
  </acceptance_criteria>
  <done>pump_ticks 테이블과 idx_pump_ticks_pump_id 인덱스가 init_db() 실행 시 생성되며, pump_log 스키마는 변경되지 않았다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: log_tick / get_ticks 함수 추가 (Phase 2 의존 계약 — 시그니처 동결)</name>
  <files>bithumb/db.py</files>
  <read_first>
    - bithumb/db.py (L168 log_pump, L180 update_pump_path, L217 get_trades — INSERT/SELECT 패턴 복제용)
    - .planning/phases/01-tick-recording-infrastructure/01-RESEARCH.md ("Code Examples: log_tick / get_ticks" L230~264)
  </read_first>
  <behavior>
    - Test 1: `log_tick(pump_id=1, seq=0, recv_ts=1000.0, price=100.0)` 호출 후 `get_ticks(1)`이 길이 1 리스트 반환, `row['exchange_ts']==1000.0` (None이면 recv_ts 복사), `row['ts_estimated']==1`
    - Test 2: `log_tick(pump_id=1, seq=1, recv_ts=1010.0, price=101.0, exchange_ts=1009.5, acc_value=5000.0, volume_power=120.0, gap_before=True, ts_estimated=False)` 호출 후 해당 행의 `exchange_ts==1009.5`, `ts_estimated==0`, `gap_before==1`, `acc_value==5000.0`, `volume_power==120.0`
    - Test 3: `get_ticks(1)`이 seq 오름차순(0, 1)으로 정렬 반환, `get_ticks(999)`(존재하지 않는 pump_id)는 빈 리스트 `[]` 반환
  </behavior>
  <action>
    `bithumb/db.py`의 `update_pump_path`(L180~) 함수 정의 바로 아래에 아래 두 함수를 추가한다. 기존 `log_pump` / `get_trades`의 스타일(모듈 레벨 함수, `with _conn() as con:`, `sqlite3.Row` → `dict`)을 그대로 따른다:

    ```python
    def log_tick(pump_id: int, seq: int, recv_ts: float, price: float,
                 exchange_ts: float | None = None, acc_value: float | None = None,
                 volume_power: float | None = None, gap_before: bool = False,
                 ts_estimated: bool = False) -> None:
        """펌핑 이벤트 1틱을 pump_ticks 에 기록. pump_id 는 pump_log.id 참조.

        exchange_ts 가 None 이면 recv_ts 를 복사하고 ts_estimated 를 True 로 강제한다.
        [Phase 2 의존 계약] 이 시그니처는 백테스트 엔진이 import 한다 — 변경 금지.
        위치 인자 4개(pump_id, seq, recv_ts, price) 고정, 나머지는 키워드 인자.
        """
        if exchange_ts is None:
            exchange_ts = recv_ts
            ts_estimated = True
        with _conn() as con:
            con.execute(
                """INSERT INTO pump_ticks
                   (pump_id, seq, exchange_ts, recv_ts, price, acc_value,
                    volume_power, gap_before, ts_estimated)
                   VALUES (?,?,?,?,?,?,?,?,?)""",
                (pump_id, seq, exchange_ts, recv_ts, price, acc_value,
                 volume_power, int(gap_before), int(ts_estimated)),
            )


    def get_ticks(pump_id: int) -> list[dict]:
        """특정 펌핑 이벤트의 모든 틱을 seq 순으로 반환 (Phase 2 백테스트용).

        [Phase 2 의존 계약] get_ticks(pump_id) -> list[dict]. 변경 금지.
        존재하지 않는 pump_id 는 빈 리스트를 반환한다.
        """
        with _conn() as con:
            rows = con.execute(
                "SELECT * FROM pump_ticks WHERE pump_id = ? ORDER BY seq", (pump_id,)
            ).fetchall()
        return [dict(r) for r in rows]
    ```

    추가로, 위 behavior의 Test 1~3을 검증하는 일회성 테스트 스크립트를 `scripts/_tick_test.py`에 작성한다 (코드베이스의 `_` 프리픽스 테스트 스크립트 관례 — CONVENTIONS.md L10). 스크립트는 `sys.path.insert(0, ...)`로 부모 경로 추가 후 `from bithumb.db import init_db, log_tick, get_ticks`를 import하고, 임시 pump_id(예: 99999)로 INSERT 후 `get_ticks`로 검증, 검증 끝나면 `DELETE FROM pump_ticks WHERE pump_id=99999`로 정리한다.
  </action>
  <verify>
    <automated>python scripts/_tick_test.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def log_tick" bithumb/db.py` 가 1
    - `grep -c "def get_ticks" bithumb/db.py` 가 1
    - `grep "def log_tick" bithumb/db.py` 출력에 `pump_id: int, seq: int, recv_ts: float, price: float` 위치 인자 4개가 정확히 이 순서로 포함
    - `grep "Phase 2 의존 계약" bithumb/db.py` 가 2줄 매치 (log_tick, get_ticks 두 docstring)
    - `python scripts/_tick_test.py` 실행 시 종료 코드 0, 출력에 "OK" 포함
    - 테스트 스크립트가 pump_id=99999 행을 검증 후 DELETE로 정리 (`grep "DELETE FROM pump_ticks" scripts/_tick_test.py` 매치)
  </acceptance_criteria>
  <done>log_tick / get_ticks가 db.py에 추가되고, exchange_ts None 폴백·gap_before·seq 정렬·빈 결과가 _tick_test.py로 검증 통과한다. 시그니처가 Phase 2 계약으로 동결됐다.</done>
</task>

</tasks>

<verification>
- `init_db()` 호출 후 `PRAGMA table_info(pump_ticks)`로 10개 컬럼 + `idx_pump_ticks_pump_id` 인덱스 확인
- `python scripts/_tick_test.py`가 종료 코드 0
- `pump_log` 테이블 스키마와 `log_pump`/`update_pump_path`/`get_trades` 함수가 일절 변경되지 않음 (git diff로 pump_log 관련 줄 변경 0 확인)
- 기존 분석 스크립트(`scripts/show_pnl.py` 등)가 import 에러 없이 동작 (`python -c "import bithumb.db"` 성공)
</verification>

<success_criteria>
- pump_ticks 테이블이 exchange_ts/recv_ts 분리 컬럼(REC-03), gap_before 플래그(REC-04)를 포함해 생성된다
- log_tick으로 틱이 INSERT되고 SELECT COUNT(*)로 증가 확인 가능(REC-02 토대)
- get_ticks가 seq 순 정렬 결과를 반환 — Phase 2 백테스트 계약 충족
- pump_log 불변 — 기존 분석 스크립트 무중단
</success_criteria>

<output>
완료 후 `.planning/phases/01-tick-recording-infrastructure/01-01-SUMMARY.md`를 생성한다.
</output>

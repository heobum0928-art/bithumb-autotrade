---
phase: 01-tick-recording-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - scripts/alt_monitor.py
  - scripts/_pump_tick_sim.py
autonomous: false
requirements: [REC-02, REC-03, REC-04]
must_haves:
  truths:
    - "펌핑 감지 후 start_pump_tracker 루프가 10초마다 pump_ticks에 틱 행을 INSERT한다"
    - "각 틱 행에 exchange_ts(거래소 시각)와 recv_ts(수신 시각)가 분리 저장된다"
    - "각 틱 행에 acc_value(누적 거래대금)가 저장된다 (D-08 — get_latest_acc_value 경유)"
    - "직전 틱과의 recv_ts 간격이 GAP_THRESHOLD_SEC를 넘으면 그 틱의 gap_before=1로 기록된다"
    - "펌핑 1건의 틱 추적이 10분간 지속되어 약 60틱이 쌓인다"
    - "pump_log 5분 집계(price_5m 등)는 기존대로 5분에 저장되고 update_pump_path는 변경되지 않는다"
  artifacts:
    - path: "scripts/alt_monitor.py"
      provides: "start_pump_tracker 루프 내 log_tick 호출 + 갭 판정 + 10분 추적"
      contains: "log_tick("
    - path: "scripts/_pump_tick_sim.py"
      provides: "틱 기록 경로 + start_pump_tracker 루프 wiring 검증 스크립트"
      contains: "log_tick"
  key_links:
    - from: "start_pump_tracker _run 루프"
      to: "bithumb.db.log_tick"
      via: "10초 루프 내 INSERT 호출"
      pattern: "log_tick\\("
    - from: "start_pump_tracker"
      to: "PriceTracker.get_latest_exchange_ts"
      via: "거래소 시각 조회"
      pattern: "get_latest_exchange_ts"
    - from: "start_pump_tracker"
      to: "PriceTracker.get_latest_acc_value"
      via: "누적 거래대금 조회 후 log_tick(acc_value=...) 전달"
      pattern: "get_latest_acc_value"
---

<objective>
펌핑 추적 루프(`start_pump_tracker`)가 매 10초 사이클마다 `log_tick()`을 호출해 `pump_ticks`에 틱을 축적하도록 배선하고, WS 갭 판정(`gap_before`)과 추적 시간 5분→10분 연장을 구현한다.

Purpose: REC-02(틱 DB 저장) + REC-03(시각 분리 기록 완성) + REC-04(갭 감지). Plan 01의 `log_tick`과 Plan 02의 `get_latest_exchange_ts`/`get_latest_acc_value`를 실제 데이터 흐름에 연결하는 wiring 단계.
Output: `scripts/alt_monitor.py`의 `start_pump_tracker` 루프에 틱 INSERT, `GAP_THRESHOLD_SEC` 상수, 갭 판정 로직, 추적 시간 10분 연장. `scripts/_pump_tick_sim.py` 검증 스크립트. `update_pump_path`와 5분 집계 로직은 불변.

NOTE: `start_pump_tracker`는 매매 봇의 핵심 추적 루프다 — 동작 검증을 위해 `autonomous: false`(human-verify 체크포인트 포함).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tick-recording-infrastructure/01-CONTEXT.md
@.planning/phases/01-tick-recording-infrastructure/01-RESEARCH.md
@.planning/phases/01-tick-recording-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-tick-recording-infrastructure/01-02-SUMMARY.md

<interfaces>
<!-- Plan 01/02가 제공하는 계약 (실행자는 이 시그니처를 그대로 호출) -->

Plan 01 (bithumb/db.py):
```python
def log_tick(pump_id: int, seq: int, recv_ts: float, price: float,
             exchange_ts: float | None = None, acc_value: float | None = None,
             volume_power: float | None = None, gap_before: bool = False,
             ts_estimated: bool = False) -> None
```
- exchange_ts가 None이면 내부적으로 recv_ts 복사 + ts_estimated=True 처리됨.

Plan 02 (scripts/alt_monitor.py PriceTracker):
```python
def get_latest_price(self, coin: str) -> float                  # 기존
def get_vol_power(self, coin: str) -> float                      # 기존
def get_latest_exchange_ts(self, coin: str) -> float | None      # Plan 02 추가
def get_latest_acc_value(self, coin: str) -> float | None        # Plan 02 추가
```

<!-- start_pump_tracker 현재 구조 (scripts/alt_monitor.py L174~254) -->

`start_pump_tracker(tracker)` 내부 `_run()`:
- item 튜플 구조 (L176~179, 13원소):
  `[pump_id(0), coin(1), base_price(2), start_time(3), peak_price(4), peak_at_sec(5), min_price_after_peak(6), pullback_hit(7), bounce_hit(8), done_1m(9), done_2m(10), done_3m(11), done_5m(12)]`
- `queue_pump` (L323~333)가 이 13원소 리스트를 `_pump_queue`에 put.
- 루프: `_pump_queue`에서 pending에 추가 → 각 item마다 `p = tracker.get_latest_price(coin)` → 가격/낙폭/시간별집계 처리 → `updates` dict를 `update_pump_path(pid, **updates)`로 저장 (L242~246 try/except).
- **종료 조건 (L248):** `if not item[12]: still.append(item)` — done_5m(item[12])이 True면 추적 종료. 현재 5분 추적.
- `time.sleep(10)` (L252).

`update_pump_path` (bithumb/db.py L180) — 절대 수정 금지 (D-09, STATE.md 제약). 5분 집계(price_5m, peak_price 등) 저장 로직(L232~240)도 그대로 둔다.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 추적 5분→10분 연장 + item 튜플에 틱 상태 필드 추가</name>
  <files>scripts/alt_monitor.py</files>
  <read_first>
    - scripts/alt_monitor.py (L174~254 start_pump_tracker 전체, L323~333 queue_pump)
    - .planning/phases/01-tick-recording-infrastructure/01-RESEARCH.md ("Pitfall 4: 5분→10분 연장 부작용" L216~220, "Pitfall 5: seq 채번" L222~226)
  </read_first>
  <action>
    D-05: 펌핑 1건당 틱 추적을 10분 지속(10초×60 = 약 60틱). 단 RESEARCH Pitfall 4 — `pump_log` 5분 집계(price_5m 등)는 **5분에 그대로 저장**하고 루프 종료만 10분으로 분리한다.

    1. **item 튜플 확장** — `queue_pump` (L323~333)가 put하는 13원소 리스트 끝에 틱 추적용 2개 필드를 추가해 15원소로 만든다. 인덱스 13 = `tick_seq`(다음 INSERT의 seq, 초기 0), 인덱스 14 = `last_recv_ts`(직전 틱 recv_ts, 초기 0.0):
    ```python
    _pump_queue.put([pump_id, coin, base_price, now,
                     base_price, 0, 0.0,
                     False, False,
                     False, False, False, False,
                     0, 0.0])   # [13]=tick_seq, [14]=last_recv_ts
    ```
    `queue_pump`의 주석(L326~329 item 구조 설명)도 두 필드를 추가해 갱신한다.

    2. **_run() 언패킹 갱신** — `start_pump_tracker` L191~193의 13원소 언패킹을 15원소로 확장하거나, 끝에 `*_`로 흡수하지 말고 명시적으로:
    ```python
    (pid, coin, base, t0, peak, peak_sec,
     min_after_peak, pullback_hit, bounce_hit,
     d1, d2, d3, d5, tick_seq, last_recv) = item
    ```

    3. **종료 조건 분리 (L248)** — 현재 `if not item[12]: still.append(item)`(done_5m 종료)를 elapsed 기반 10분 종료로 변경:
    ```python
    if elapsed < 600:   # 10분 미만이면 계속 추적 (5분 집계는 그대로 저장됨)
        still.append(item)
    ```
    `item[12]`(done_5m) 플래그와 5분 집계 저장 블록(L232~240의 `if not d5 and elapsed >= 300:` 및 `updates["peak_price"]` 등)은 **그대로 유지** — 5분 시점에 `update_pump_path`로 집계가 저장되고, 루프는 10분까지 계속 돈다. `update_pump_path`는 한 줄도 수정하지 않는다.

    4. 상수 영역(L58 부근)에 추적 시간 상수를 추가(가독성·D-12 상수 관리 일관성):
    ```python
    PUMP_TRACK_SEC   = 600   # 펌핑 틱 추적 지속시간 10분 (D-05)
    ```
    종료 조건 `elapsed < 600`을 `elapsed < PUMP_TRACK_SEC`로 쓴다.
  </action>
  <verify>
    <automated>python -c "s=open('scripts/alt_monitor.py',encoding='utf-8').read(); assert 'PUMP_TRACK_SEC' in s; assert 'tick_seq' in s and 'last_recv' in s; assert s.count(', 0, 0.0])')>=1 or '0, 0.0]' in s; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PUMP_TRACK_SEC" scripts/alt_monitor.py` 가 2 이상 (상수 정의 + 종료 조건 사용)
    - `grep "PUMP_TRACK_SEC =" scripts/alt_monitor.py` 가 값 `600` 포함
    - `queue_pump`의 `_pump_queue.put([...])` 리스트가 15원소 (끝에 `0, 0.0` 추가 — tick_seq, last_recv_ts)
    - `start_pump_tracker` _run의 item 언패킹이 15개 변수 (`tick_seq`, `last_recv` 포함)
    - 종료 조건이 `elapsed`(또는 `< PUMP_TRACK_SEC`) 기반 — `if not item[12]:` 단독 종료 조건이 제거됨
    - `update_pump_path` 호출과 5분 집계 블록(`elapsed >= 300`, `price_5m`)은 git diff에서 미변경
  </acceptance_criteria>
  <done>펌핑 추적이 10분간 지속되도록 종료 조건이 elapsed 기반으로 바뀌었고, item 튜플에 tick_seq/last_recv_ts 필드가 추가됐다. 5분 집계 저장과 update_pump_path는 불변이다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 틱 기록 경로 + 루프 wiring 검증 스크립트 작성 (_pump_tick_sim.py)</name>
  <files>scripts/_pump_tick_sim.py</files>
  <read_first>
    - scripts/alt_monitor.py (L323~333 queue_pump, L174~254 start_pump_tracker — 변경 반영 상태)
    - bithumb/db.py (log_pump, log_tick, get_ticks — 검증에 사용)
    - .planning/phases/01-tick-recording-infrastructure/01-RESEARCH.md ("Environment Availability" L337~349 — 실제 펌핑 없이 INSERT 경로를 검증하라는 권고)
  </read_first>
  <action>
    실제 펌핑 이벤트는 즉시 발생하지 않으므로(RESEARCH Environment Availability), 틱 기록 경로 + start_pump_tracker 루프 wiring을 봇 프로세스·실제 펌핑 없이 검증하는 일회성 스크립트를 먼저 작성한다. Task 3(log_tick 배선)의 verify가 이 스크립트를 호출하므로, **이 Task는 Task 3보다 먼저 실행되어야 한다** (Plan 내 번호 순서가 곧 실행 순서).

    코드베이스의 `_` 프리픽스 테스트 스크립트 관례(CONVENTIONS.md L10)를 따른다. `scripts/_pump_tick_sim.py`는 두 검증 파트로 구성한다:

    **Part A — log_tick / get_ticks 계약 + 갭 판정 직접 검증:**
    1. `sys.path.insert(0, str(Path(__file__).parent.parent))` 후 `from bithumb.db import init_db, log_pump, log_tick, get_ticks, _conn` import.
    2. `init_db()` 호출.
    3. 테스트용 pump_log 행을 생성: `log_pump`로 펌핑 1건 등록 후, 방금 INSERT된 `pump_log.id`를 `SELECT MAX(id) FROM pump_log`로 얻는다 (test_pump_id).
    4. 갭 판정 로직을 직접 재현해 `log_tick`을 여러 번 호출 — 정상 간격 틱 3개(seq 0,1,2; recv_ts 1000,1010,1020; gap_before=False) + 갭 틱 1개(seq 3; recv_ts 1060 → 직전 1020과 40초 간격 ≥ 30 → gap_before=True). exchange_ts는 일부는 값 전달, 일부는 None(폴백)으로. `acc_value`도 일부 틱에 값 전달.
    5. `get_ticks(test_pump_id)`로 4개 틱을 읽어 assert: 길이 4, seq 정렬, seq=3 행의 `gap_before==1`, exchange_ts None으로 넣은 행의 `ts_estimated==1`, acc_value 전달한 행의 값 일치.

    **Part B — start_pump_tracker 루프 wiring 검증 (Task 4 체크포인트 부담 경감, REC verification_derivation 보강):**
    실제 펌핑·실제 WS 없이 루프 1사이클의 wiring을 검증한다 — `log_tick` 호출 경로, `tick_seq` 증가(item[13]), `last_recv` 갱신(item[14])이 올바른지 확인.
    1. `from scripts.alt_monitor import ...` 또는 `importlib`로 `queue_pump`, `start_pump_tracker`, `PriceTracker`, `_pump_queue`를 가져온다. 단, alt_monitor 모듈 레벨 부작용이 있으면 import가 무거우므로, 가능하면 `start_pump_tracker`의 루프 본문이 호출하는 함수만 좁게 가져오거나, 아래 stub 방식을 쓴다.
    2. **Stub PriceTracker 작성** — `get_latest_price(coin)`은 고정 양수(예: 150.0), `get_vol_power(coin)`은 120.0, `get_latest_exchange_ts(coin)`은 고정 epoch, `get_latest_acc_value(coin)`은 고정 거래대금(예: 5000.0)을 반환하는 최소 stub 클래스를 스크립트 안에 정의한다.
    3. `queue_pump`로 Part A의 test_pump_id에 대한 펌핑 item을 큐에 넣는다 (15원소 리스트가 들어가는지 간접 확인).
    4. `start_pump_tracker(stub)`를 별도 스레드(daemon)로 시작하거나, 루프 본문 1사이클을 직접 추출해 호출한다 — **목표는 `sleep(10)` 전체 대기 없이** 최소 1사이클이 돌게 하는 것. 안전한 방법: 루프가 daemon 스레드면 약 12초 후 검증하고, 또는 (권장) 루프 본문에서 `log_tick` 호출 경로를 그대로 재현하는 헬퍼를 alt_monitor가 노출하지 않는다면 Part B는 "queue_pump가 15원소 item을 put + start_pump_tracker가 stub의 get_latest_acc_value를 호출" 만 검증해도 된다.
    5. 1사이클 후 `get_ticks(test_pump_id)`로 새 틱이 1개 이상 INSERT됐는지, 그 틱의 `acc_value`가 stub 반환값(5000.0)과 일치하는지, `exchange_ts`가 stub 반환값과 일치하는지 assert. seq가 0부터 채번됐는지 확인. (Part A 데이터와 섞이지 않도록 별도 test_pump_id를 쓰거나 Part A 정리 후 Part B 실행.)
    6. Part B가 환경 제약(모듈 import 불가 등)으로 완전 실행이 어려우면, 최소한 `queue_pump` 호출 → `_pump_queue` 크기 증가 + put된 item이 15원소임을 assert하는 것으로 축소하되, 그 한계를 스크립트 주석과 출력에 명시한다.

    **정리:** 검증 후 `DELETE FROM pump_ticks WHERE pump_id IN (...)` + `DELETE FROM pump_log WHERE id IN (...)` (사용한 모든 test_pump_id) — 실제 분석 데이터 오염 방지.

    전부 통과하면 `print("OK")`, 실패 시 `AssertionError`로 비정상 종료.
  </action>
  <verify>
    <automated>python scripts/_pump_tick_sim.py</automated>
  </verify>
  <acceptance_criteria>
    - 파일 `scripts/_pump_tick_sim.py`가 존재
    - `grep -c "from bithumb.db import" scripts/_pump_tick_sim.py` 가 1 이상, `log_tick`·`get_ticks`·`log_pump` import 포함
    - `grep -c "DELETE FROM pump_ticks" scripts/_pump_tick_sim.py` 가 1 이상, `grep -c "DELETE FROM pump_log" scripts/_pump_tick_sim.py` 가 1 이상 (정리 로직)
    - `grep -c "gap_before" scripts/_pump_tick_sim.py` 가 2 이상 (갭 틱 INSERT + assert)
    - `grep -c "get_latest_acc_value" scripts/_pump_tick_sim.py` 가 1 이상 (stub 또는 wiring 검증)
    - Part B 루프 wiring 검증 로직 존재 — stub PriceTracker 또는 queue_pump/_pump_queue 검증 (`grep -c "queue_pump\|start_pump_tracker" scripts/_pump_tick_sim.py` ≥ 1)
    - `python scripts/_pump_tick_sim.py` 실행 시 종료 코드 0, 출력에 "OK"
  </acceptance_criteria>
  <done>_pump_tick_sim.py가 log_tick INSERT, get_ticks 읽기, 갭 판정(gap_before), exchange_ts/acc_value 전달을 봇 없이 검증하고, start_pump_tracker 루프 wiring(queue_pump → log_tick 경로)을 stub로 1사이클 검증하며, 테스트 데이터를 정리한다. Task 3 작성 전이라 이 시점엔 log_tick 미배선 — 스크립트는 Part A를 우선 통과시키고 Part B는 Task 3 완료 후 재실행 시 통과한다 (verify는 Task 3 완료 후 최종 확인).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 10초 루프에 log_tick 호출 + WS 갭 판정 + acc_value 배선 (REC-02/03/04, D-08)</name>
  <files>scripts/alt_monitor.py</files>
  <read_first>
    - scripts/alt_monitor.py (L174~254 start_pump_tracker — Task 1 변경 반영된 상태, L1~30 import 영역)
    - .planning/phases/01-tick-recording-infrastructure/01-01-SUMMARY.md (log_tick 시그니처)
    - .planning/phases/01-tick-recording-infrastructure/01-02-SUMMARY.md (get_latest_exchange_ts / get_latest_acc_value 시그니처)
    - .planning/phases/01-tick-recording-infrastructure/01-RESEARCH.md ("Pitfall 1: WS 무음 단절" L197~202, "Code Examples: start_pump_tracker 틱 INSERT" L290~307)
  </read_first>
  <action>
    1. **import 추가** — `scripts/alt_monitor.py` 상단 `from bithumb.db import ...` 줄에 `log_tick`을 추가한다 (`update_pump_path`가 이미 import돼 있을 것 — 그 옆에).

    2. **GAP_THRESHOLD_SEC 상수** — 상수 영역(L58 부근, PUMP_TRACK_SEC 옆)에 추가:
    ```python
    GAP_THRESHOLD_SEC = 30   # 직전 틱과 recv_ts 간격이 이 값 이상이면 WS 갭으로 판정 (D-12, 예상 10초의 3배)
    ```

    3. **log_tick 호출 배선** — `start_pump_tracker` `_run()` 루프 내, `update_pump_path(pid, **updates)` 호출 부근(L242~246 try/except 옆)에 틱 INSERT를 추가한다. `p = tracker.get_latest_price(coin)`로 이미 가격을 얻은 상태(L195). 거래량·체결강도·거래소시각·누적거래대금은 tracker getter로 조회:
    ```python
    # ── 틱 기록 (REC-02/03/04, D-08) — 추가 API 호출 없음, tracker 버퍼 재사용 ──
    if p > 0:
        now_ts = now            # 루프 상단의 now = time.time()
        gap = bool(last_recv) and (now_ts - last_recv) >= GAP_THRESHOLD_SEC
        ex_ts  = tracker.get_latest_exchange_ts(coin)   # None 가능 → log_tick이 폴백 처리
        vp     = tracker.get_vol_power(coin)
        acc_v  = tracker.get_latest_acc_value(coin)     # None 가능 → 컬럼 NULL 허용
        try:
            log_tick(pid, tick_seq, recv_ts=now_ts, price=p,
                     exchange_ts=ex_ts, acc_value=acc_v, volume_power=vp,
                     gap_before=gap)
        except Exception:
            pass
        item[13] = tick_seq + 1   # 다음 seq
        item[14] = now_ts         # last_recv_ts 갱신
    ```
    설계 메모:
    - **acc_value 배선 (BLOCKER 수정 — D-08 완전 이행):** Plan 02가 `PriceTracker.get_latest_acc_value(coin)` getter를 추가했다. `start_pump_tracker`가 이 getter로 누적 거래대금을 조회해 `log_tick(acc_value=acc_v)`로 전달한다. CONTEXT는 거래량 기반 *전략*을 deferred 했을 뿐 거래량 *데이터* 수집은 D-08이 명시적으로 요구한다 — WS가 제공하는 세 값(closePrice·value·volumePower)을 모두 저장해야 향후 거래량 백테스트 여지가 남는다. getter가 None을 반환하면(미수신 코인) `log_tick`이 컬럼을 NULL로 둔다 — 정상 동작.
    - 갭 판정(REC-04): `last_recv`(item[14])와 현재 `now`의 간격이 `GAP_THRESHOLD_SEC` 이상이면 `gap_before=1`. 첫 틱(`last_recv==0`)은 `bool(last_recv)` False라 갭 아님.
    - `ts_estimated`는 `log_tick`이 `exchange_ts is None`일 때 자동으로 1 처리하므로 명시 전달 불필요.
    - try/except로 감싸 틱 INSERT 실패가 추적 루프를 멈추지 않게 함 (기존 `update_pump_path` 패턴 — RESEARCH Anti-Pattern).

    4. **`item[13]`/`item[14]` 갱신 위치** — Task 1에서 언패킹한 `tick_seq`/`last_recv`는 로컬 변수이므로, 다음 사이클에 반영하려면 반드시 위처럼 `item[13]`/`item[14]`에 다시 써야 한다. `still.append(item)` 전에 갱신되어야 한다.

    `update_pump_path` 호출, 5분 집계, 가격/낙폭 추적 로직은 일절 수정하지 않는다.
  </action>
  <verify>
    <automated>python scripts/_pump_tick_sim.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "log_tick(" scripts/alt_monitor.py` 가 1 이상 (호출부)
    - `grep "from bithumb.db import" scripts/alt_monitor.py` 출력에 `log_tick` 포함
    - `grep -c "GAP_THRESHOLD_SEC" scripts/alt_monitor.py` 가 2 이상 (상수 정의 + 갭 판정 사용)
    - `grep "GAP_THRESHOLD_SEC =" scripts/alt_monitor.py` 가 값 `30` 포함
    - `grep -c "get_latest_acc_value" scripts/alt_monitor.py` 가 1 이상 (acc_value 배선)
    - log_tick 호출에 `acc_value=` 키워드 인자가 전달됨 (`grep "log_tick(" -A3 scripts/alt_monitor.py`로 확인)
    - log_tick 호출이 `try:` / `except Exception:` 로 감싸짐
    - `item[13]`, `item[14]` 갱신 줄이 존재 (tick_seq/last_recv_ts 영속화)
    - `get_latest_exchange_ts` 호출이 존재
    - `python scripts/_pump_tick_sim.py`가 종료 코드 0, 출력에 "OK" — Part A + Part B(루프 wiring) 모두 통과
  </acceptance_criteria>
  <done>start_pump_tracker 10초 루프가 매 사이클 log_tick을 호출해 pump_ticks에 틱을 INSERT하고, recv_ts 간격으로 gap_before를 판정하며, 거래소 시각·누적 거래대금(acc_value)을 함께 저장한다. update_pump_path는 불변이다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 라이브 봇 틱 축적 + 타임존 정합성 검증</name>
  <action>
    사용자 검증 체크포인트. 자동 코드 변경 없음 — 루프 wiring·INSERT 경로는 Task 2의 `_pump_tick_sim.py`(Part B 포함)가 봇 없이 자동 검증한다. 이 체크포인트는 자동 검증으로 확인 불가능한 **타임존 정합성**(exchange_ts vs recv_ts 델타)만 사람이 확인한다 — 실제 빗썸 WS의 KST date/time이 합리적인 epoch로 변환되는지는 라이브 메시지로만 검증 가능하다.
  </action>
  <verify>사용자가 라이브 봇(또는 1회성 WS 덤프)에서 exchange_ts와 recv_ts의 차이가 합리적 범위(수백 ms~수초)임을 확인한 뒤 "approved" 응답. 펌핑 이벤트 발생을 무한정 기다릴 필요 없음 — 루프 wiring은 _pump_tick_sim.py로 이미 검증됨.</verify>
  <done>exchange_ts/recv_ts 타임존 정합성(델타가 +9h/-9h 오류가 아님)을 사용자가 확인했다. 루프 wiring·INSERT·갭 경로는 _pump_tick_sim.py 자동 검증으로 완료됐다.</done>
  <what-built>
    start_pump_tracker 루프가 펌핑 감지 시 10초마다 pump_ticks에 틱을 INSERT한다. 거래소 시각/수신 시각 분리 기록, acc_value 저장, WS 갭 플래그, 10분 추적이 동작한다. `_pump_tick_sim.py`가 INSERT/읽기/갭 경로 + start_pump_tracker 루프 wiring(stub PriceTracker로 1사이클)을 봇 없이 자동 검증했다. 이 체크포인트는 자동으로 검증 불가능한 항목 — 빗썸 WS의 KST date/time이 올바른 epoch로 변환되는 **타임존 정합성** — 만 사람이 확인한다.
  </what-built>
  <how-to-verify>
    1. (Plan 02 완료 전제) config.yaml에 `trading.record_only: true`가 설정돼 있는지 확인 — 실거래 차단 상태에서 검증한다.
    2. **타임존 정합성 확인 (이 체크포인트의 핵심):** 다음 중 하나로 빗썸 WS의 실제 거래소 시각을 epoch로 변환했을 때 수신 시각과 가까운지 확인한다.
       - (빠른 방법) 일회성 WS 덤프: `_signal_check.py` 같은 스크립트 또는 `on_message`에 임시 `log.debug`를 넣어 실제 `content`의 `date`/`time` 1건을 얻고, `_parse_exchange_ts`로 변환한 값과 `time.time()`을 비교한다. 차이가 수백 ms~수초면 정상.
       - (운영 검증) 봇을 실행해 펌핑이 1회 감지된 후 DB 확인:
         `python -c "import sqlite3; c=sqlite3.connect('data/trades.db'); rows=c.execute('SELECT exchange_ts, recv_ts, ts_estimated FROM pump_ticks ORDER BY id DESC LIMIT 5').fetchall(); [print('delta=%.2fs ts_est=%d' % (r[1]-r[0], r[2])) for r in rows]"`
    3. 확인 사항:
       - `exchange_ts`와 `recv_ts`의 차이(delta)가 합리적인가? 수백 ms~수초 이내면 정상. **+32400초(±9h)면 타임존 오류** → `_parse_exchange_ts`의 KST 가정이 틀림, 상수 한 줄 수정 필요 (RESEARCH Open Question 2).
       - `ts_estimated`가 대부분 0인가? (date/time 파싱 성공 시 0. 전부 1이면 WS에 date/time 필드 없음 → RESEARCH Open Question 1 재검증 필요).
    4. (선택) 펌핑이 실제로 감지됐다면 `SELECT COUNT(*) FROM pump_ticks`가 10초마다 증가하고 `acc_value`가 채워지는지 함께 확인한다 — 단 펌핑 발생을 무한정 기다릴 필요는 없다. 루프 wiring은 _pump_tick_sim.py Part B가 이미 검증했다.
  </how-to-verify>
  <resume-signal>exchange_ts/recv_ts 델타가 합리적 범위(타임존 오류 아님)임을 확인하면 "approved" 입력, 또는 발견한 문제(±9h 타임존 오류, ts_estimated 전부 1 등)를 알려주세요.</resume-signal>
</task>

</tasks>

<verification>
- `python scripts/_pump_tick_sim.py`가 종료 코드 0 (INSERT/읽기/갭/폴백 경로 + start_pump_tracker 루프 wiring)
- start_pump_tracker 루프 wiring이 stub PriceTracker로 자동 검증됨 — log_tick 호출·seq 채번·acc_value 전달
- 틱 행에 exchange_ts/recv_ts 분리 저장 + acc_value 저장 (REC-03 성공기준 3, D-08)
- WS 단절 구간 틱에 gap_before=1 (REC-04 성공기준 4)
- exchange_ts/recv_ts 타임존 정합성을 라이브 검증으로 확인 (Task 4 체크포인트)
- pump_log 5분 집계·update_pump_path 불변 — 기존 분석 스크립트 무중단 (D-09)
</verification>

<success_criteria>
- 펌핑 감지 시 10초마다 pump_ticks에 틱이 INSERT되어 약 60틱/이벤트 축적 (REC-02)
- 거래소 시각/수신 시각이 분리 기록, exchange_ts 미가용 시 ts_estimated=1 폴백 (REC-03)
- acc_value(누적 거래대금)가 매 틱에 저장됨 — D-08 완전 이행, 향후 거래량 백테스트 데이터 확보
- recv_ts 간격 기반 gap_before 플래그로 WS 갭이 명시 기록됨 (REC-04)
- 루프 wiring이 _pump_tick_sim.py로 자동 검증, 사람 체크포인트는 타임존 정합성만 담당
- pump_log 스키마·update_pump_path 불변, 기존 분석 스크립트 정상
</success_criteria>

<output>
완료 후 `.planning/phases/01-tick-recording-infrastructure/01-03-SUMMARY.md`를 생성한다. SUMMARY에 acc_value 수집 상태(D-08 이행 완료)와 라이브 타임존 검증 결과를 명시한다.
</output>
</content>
</invoke>

---
phase: 01-tick-recording-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/alt_monitor.py
autonomous: false
requirements: [REC-01, REC-03]
user_setup:
  - service: config.yaml
    why: "RECORD_ONLY 스위치(trading.record_only)를 봇이 읽는다. config.yaml은 git 커밋 금지이므로 사용자가 로컬 파일에 키를 추가해야 한다"
    dashboard_config:
      - task: "config.yaml의 trading: 섹션에 record_only: true 추가 (없으면 trading: 섹션 신설)"
        location: "프로젝트 루트 config.yaml (git 비추적)"
must_haves:
  truths:
    - "RECORD_ONLY가 true일 때 do_buy/do_buy_limit/do_sell이 주문 API를 호출하지 않고 None을 반환한다"
    - "차단 시마다 로그에 'RECORD_ONLY 차단됨' 메시지가 기록된다"
    - "신호 감지·필터 로직은 그대로 동작하며 signal_log/pump_log 기록은 계속된다"
    - "WS on_message가 content의 date/time을 파싱해 거래소 시각을 epoch로 보관한다"
    - "거래소 시각을 코인별로 조회하는 get_latest_exchange_ts()가 존재한다"
  artifacts:
    - path: "scripts/alt_monitor.py"
      provides: "RECORD_ONLY 상수 + 3개 매매함수 게이트 + exchange_ts 파싱 + getter"
      contains: "RECORD_ONLY 차단됨"
  key_links:
    - from: "do_buy/do_buy_limit/do_sell"
      to: "RECORD_ONLY 상수"
      via: "함수 첫 줄 가드 클로즈"
      pattern: "if RECORD_ONLY:"
    - from: "PriceTracker.on_message"
      to: "_parse_exchange_ts"
      via: "content date/time 파싱"
      pattern: "_parse_exchange_ts"
---

<objective>
봇을 기록 전용 모드로 전환하는 RECORD_ONLY 게이트를 매매 함수 3곳에 삽입하고, WebSocket `on_message`가 거래소 발생 시각(`date`+`time`)을 파싱·보관하도록 확장한다.

Purpose: REC-01(실거래 차단) + REC-03(거래소 시각 분리 기록의 입력단). 검증되지 않은 전략에 실제 돈이 들어가지 않게 하는 이번 마일스톤의 핵심 안전장치.
Output: `scripts/alt_monitor.py`에 `RECORD_ONLY` 상수, `do_buy`/`do_buy_limit`/`do_sell` 가드 클로즈, `_parse_exchange_ts` 헬퍼, `PriceTracker`의 `exchange_ts` 보관 + `get_latest_exchange_ts()` getter.

NOTE: 이 plan은 매매 코드 변경(do_buy/do_sell)을 포함한다 — CLAUDE.md 규칙상 사용자 검토 게이트가 필요해 `autonomous: false`다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tick-recording-infrastructure/01-CONTEXT.md
@.planning/phases/01-tick-recording-infrastructure/01-RESEARCH.md

<interfaces>
<!-- scripts/alt_monitor.py 관련 코드 좌표 (실행자는 탐색 불필요) -->

- L58: `WS_URL = "wss://pubwss.bithumb.com/pub/ws"` — 상수 영역. RECORD_ONLY 상수를 이 부근에 추가.
- L429~628: `class PriceTracker` — `MAXLEN=60`, `self._hist: dict[str, deque]`, `self._vol_power: dict`, `self._lock`.
- L448~472: `on_message` — `c = data.get("content", {})`, `closePrice`/`value`/`volumePower` 파싱. deque에 `hist.append((now, price, acc_val))` — **3원소 튜플**.
- L506~508: `get_vol_power(coin)` — `_vol_power` getter. 같은 패턴으로 `get_latest_exchange_ts` 추가.
- L516, L524: `get_signal` 내 `now_ts, now_price, now_vol = snaps[-1]` 와 `old_ts, old_price, old_vol = old_snap` — 3원소 언패킹.
- L581, L601~604: `get_preemptive_signal` 내 `now_ts, now_price, _ = snaps[-1]` 등 — 3원소 언패킹.
- L622~627: `get_latest_price(coin)` — `hist[-1][1]` 반환.
- L802: `def do_buy(client, coin, buy_krw) -> dict | None:`
- L835: `def do_buy_limit(client, coin, buy_krw, ...) -> dict | None:`
- L894: `def do_sell(client, pos, volume, ...) -> float | None:`

config 로딩 패턴 (CONVENTIONS.md L222~232): `_get_cfg()` 지연 로딩.
`bithumb/notify.py`의 `_get_cfg()`가 `config.yaml`을 읽는다. alt_monitor.py가 자체 cfg 접근 방식을 쓰는지 read_first에서 확인할 것.

**중요 제약 (RESEARCH "Code Examples: on_message" L288):** deque 튜플은 `(now, price, acc_val)` 3원소이고 `get_signal`/`get_preemptive_signal`이 정확히 3원소로 언패킹한다. 튜플을 4원소로 확장하면 이 언패킹이 깨진다 → 이 plan은 **deque 튜플을 확장하지 않고**, 거래소 시각을 별도 dict(`self._ex_ts: dict[str, float]`)에 코인별로 보관한다. get_signal/get_preemptive_signal은 일절 수정하지 않는다.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: _parse_exchange_ts 헬퍼 + PriceTracker 거래소 시각 보관/getter</name>
  <files>scripts/alt_monitor.py</files>
  <read_first>
    - scripts/alt_monitor.py (L1~70 import/상수 영역, L429~510 PriceTracker 클래스 전반, L448~472 on_message, L506~508 get_vol_power, L622~628 get_latest_price)
    - .planning/phases/01-tick-recording-infrastructure/01-RESEARCH.md ("Pattern 3: 거래소 시각 파싱" L158~174, "Code Examples: on_message" L276~288, "State of the Art" L309~316 — Pro 문서 참조 금지)
  </read_first>
  <action>
    빗썸 레거시 공개 WS(`wss://pubwss.bithumb.com/pub/ws`)의 ticker `content`는 `date`(YYYYMMDD)와 `time`(HHMMSS, KST) 필드를 포함한다 (RESEARCH Open Question 1, 공식 문서 확인 HIGH 신뢰도).

    1. **모듈 레벨 헬퍼 추가** — `scripts/alt_monitor.py`의 import 영역과 상수 영역 사이(또는 다른 모듈 레벨 헬퍼 옆)에 추가. 파일 상단 import에 `from datetime import datetime, timezone, timedelta`가 없으면 기존 datetime import 줄에 병합:

    ```python
    KST = timezone(timedelta(hours=9))

    def _parse_exchange_ts(date_s: str, time_s: str) -> float | None:
        """빗썸 WS content date(YYYYMMDD)+time(HHMMSS, KST) → epoch sec. 실패 시 None."""
        try:
            dt = datetime.strptime(date_s + time_s, "%Y%m%d%H%M%S")
            return dt.replace(tzinfo=KST).timestamp()
        except (ValueError, TypeError):
            return None
    ```

    2. **PriceTracker.__init__ (L432~437)에 거래소 시각 dict 추가** — 기존 `self._vol_power: dict[str, float] = {}` 줄 바로 아래에:
    ```python
    self._ex_ts: dict[str, float] = {}   # 코인별 최근 거래소 발생 시각 (epoch sec)
    ```

    3. **on_message (L448~472) 확장** — `vp = float(c.get("volumePower", 100) or 100)` 줄 다음, `now = time.time()` 부근에 거래소 시각 파싱을 추가. deque 튜플(`hist.append((now, price, acc_val))`)은 **3원소 그대로 유지**한다. `with self._lock:` 블록 안 `self._vol_power[coin] = vp` 옆에 추가:
    ```python
    ex_ts = _parse_exchange_ts(c.get("date", ""), c.get("time", ""))
    ```
    그리고 lock 블록 안:
    ```python
    if ex_ts is not None:
        self._ex_ts[coin] = ex_ts
    ```

    4. **getter 추가** — `get_vol_power` (L506~508) 바로 아래, 동일 패턴으로:
    ```python
    def get_latest_exchange_ts(self, coin: str) -> float | None:
        """코인별 최근 거래소 발생 시각(epoch sec). 파싱 실패/미수신이면 None."""
        with self._lock:
            return self._ex_ts.get(coin)
    ```

    `get_signal`, `get_preemptive_signal`, `get_latest_price`는 일절 수정하지 않는다. deque 튜플 구조 불변.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'.'); sys.path.insert(0,'scripts'); import importlib.util; spec=importlib.util.spec_from_file_location('am','scripts/alt_monitor.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert m._parse_exchange_ts('20260519','121844') is not None; assert m._parse_exchange_ts('bad','x') is None; t=m.PriceTracker(); assert t.get_latest_exchange_ts('BTC') is None; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def _parse_exchange_ts" scripts/alt_monitor.py` 가 1
    - `grep -c "def get_latest_exchange_ts" scripts/alt_monitor.py` 가 1
    - `grep -c "self._ex_ts" scripts/alt_monitor.py` 가 3 이상 (init 선언, on_message 저장, getter 조회)
    - `grep -c "hist.append" scripts/alt_monitor.py` 결과의 append 인자가 여전히 3원소 `(now, price, acc_val)` — 4원소 튜플로 바뀌지 않음
    - `get_signal`/`get_preemptive_signal` 내 언패킹 줄(`now_ts, now_price, now_vol =`, `now_ts, now_price, _ =`)이 변경되지 않음 (git diff에 해당 줄 없음)
    - verify 스크립트가 "OK" 출력 (`_parse_exchange_ts` 정상값/실패값, getter None 동작)
  </acceptance_criteria>
  <done>_parse_exchange_ts가 KST date/time을 epoch로 변환하고, PriceTracker가 코인별 거래소 시각을 _ex_ts dict에 보관하며 get_latest_exchange_ts로 조회 가능하다. deque 튜플과 신호 함수는 불변이다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: RECORD_ONLY 상수 + do_buy/do_buy_limit/do_sell 게이트 (D-01/D-02)</name>
  <files>scripts/alt_monitor.py</files>
  <read_first>
    - scripts/alt_monitor.py (L1~70 import/상수 영역 — config 로딩 방식 확인, L802~830 do_buy, L835~890 do_buy_limit, L894~950 do_sell)
    - bithumb/notify.py (_get_cfg 지연 로딩 패턴 — config.yaml 읽는 방식)
    - .planning/phases/01-tick-recording-infrastructure/01-RESEARCH.md ("Pattern 1: RECORD_ONLY Gate" L114~133, "Pitfall 3: 재시작 시 잔여 포지션" L210~214)
  </read_first>
  <action>
    D-01: 게이트는 `do_buy`/`do_buy_limit`/`do_sell` 함수의 **첫 줄**(파라미터 검증·로직 이전). RECORD_ONLY가 켜져 있으면 `return None` + "RECORD_ONLY 차단됨" 로그. 신호 감지·필터 로직(D-03)은 손대지 않는다 — 주문 실행만 막는다.

    1. **RECORD_ONLY 상수 추가** — `scripts/alt_monitor.py` 상수 영역(L58 `WS_URL` 부근). config.yaml의 `trading.record_only`를 1회 로드, 기본값 `True`(안전). read_first에서 확인한 alt_monitor.py의 실제 config 접근 방식(`_get_cfg()` 또는 모듈 레벨 `cfg`/`CFG` 변수)을 사용한다. 해당 변수가 이미 dict로 로드돼 있으면:
    ```python
    # config.yaml trading.record_only — 기본 True(안전). 실거래 재개는 별도 마일스톤.
    RECORD_ONLY = bool(cfg.get("trading", {}).get("record_only", True))
    ```
    config가 함수로만 접근 가능하면 `_get_cfg().get("trading", {}).get("record_only", True)` 형태로. 정확한 변수명은 read_first 결과에 맞춘다 — 추측 금지.

    2. **do_buy 게이트 (L802)** — 함수 첫 줄(`market = ...` 등 본문 이전):
    ```python
    def do_buy(client: BithumbClient, coin: str, buy_krw: float) -> dict | None:
        if RECORD_ONLY:
            log.warning(f"[{coin}] RECORD_ONLY 차단됨 — 시장가 매수 {buy_krw:,.0f}원 미실행")
            return None
        ...  # 기존 본문 그대로
    ```

    3. **do_buy_limit 게이트 (L835)** — 함수 첫 줄:
    ```python
        if RECORD_ONLY:
            log.warning(f"[{coin}] RECORD_ONLY 차단됨 — 지정가 매수 {buy_krw:,.0f}원 미실행")
            return None
    ```

    4. **do_sell 게이트 (L894)** — 함수 첫 줄. `do_sell`은 `float | None`을 반환하지만 `return None`은 호환된다 (RESEARCH Pattern 1 주의):
    ```python
        if RECORD_ONLY:
            coin = pos.get("coin", "?")
            log.warning(f"[{coin}] RECORD_ONLY 차단됨 — 시장가 매도 {volume:.6f} 미실행")
            return None
    ```
    `do_sell`의 정확한 파라미터명(`pos`, `volume`)은 L894 시그니처를 read_first로 확인해 일치시킨다. 코인명을 로그에 넣되 시그니처에서 추출 가능한 값만 사용한다.

    5. 기존 신호 감지·진입 결정·필터 로직, `signal_log`/`pump_log` 기록 코드는 **한 줄도 수정하지 않는다** (D-03: 차단 중에도 신호 기록 계속).

    **RESEARCH Pitfall 3 대응 (잔여 포지션):** 코드 변경은 하지 않는다. 대신 SUMMARY.md에 운영 체크리스트로 명시한다 — "RECORD_ONLY 전환 전 `data/active_pos.json`에 잔여 포지션이 있으면 수동 청산하고 파일을 비울 것. RECORD_ONLY가 do_sell도 막으므로 잔여 포지션은 자동 청산되지 않는다."
  </action>
  <verify>
    <automated>python -c "import re; s=open('scripts/alt_monitor.py',encoding='utf-8').read(); assert s.count('RECORD_ONLY 차단됨')==3, s.count('RECORD_ONLY 차단됨'); assert re.search(r'RECORD_ONLY\s*=\s*bool', s), 'no RECORD_ONLY const'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RECORD_ONLY 차단됨" scripts/alt_monitor.py` 가 정확히 3 (do_buy, do_buy_limit, do_sell)
    - `grep -c "if RECORD_ONLY:" scripts/alt_monitor.py` 가 3 이상
    - `grep "RECORD_ONLY = bool" scripts/alt_monitor.py` 가 1줄 매치, `record_only` 키와 기본값 `True` 포함
    - `do_buy`/`do_buy_limit`/`do_sell` 각 함수에서 `if RECORD_ONLY:` 가드가 다른 본문 코드보다 먼저 등장 (함수 첫 문장)
    - `grep -c "signal_log\|log_pump" scripts/alt_monitor.py` 결과가 변경 전과 동일 (신호 기록 코드 미변경 — git diff로 확인)
    - SUMMARY.md에 "RECORD_ONLY 전환 전 active_pos.json 비우기" 운영 체크리스트가 포함
  </acceptance_criteria>
  <done>RECORD_ONLY=true일 때 세 매매 함수가 주문 없이 None을 반환하고 "RECORD_ONLY 차단됨" 로그를 남긴다. 신호 감지·기록 로직은 불변이며, 잔여 포지션 운영 절차가 SUMMARY에 문서화됐다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 매매 코드 변경 검토 + config.yaml record_only 설정 확인</name>
  <action>
    사용자 검토 체크포인트 (CLAUDE.md: 매매 코드 변경은 사용자 검토 후 진행). 자동 코드 변경 없음 — 아래 what-built / how-to-verify / resume-signal에 따라 사용자가 검토하고 config.yaml을 설정한다.
  </action>
  <verify>사용자가 `git diff scripts/alt_monitor.py`로 매매 함수 3곳을 검토하고 config.yaml에 trading.record_only: true를 설정한 뒤 "approved" 응답</verify>
  <done>사용자가 매매 코드 변경(do_buy/do_buy_limit/do_sell 게이트)을 승인했고, config.yaml에 trading.record_only: true가 설정됐다.</done>
  <what-built>
    do_buy/do_buy_limit/do_sell 3개 매매 함수에 RECORD_ONLY 가드 클로즈가 삽입됐고, WS on_message가 거래소 시각을 파싱한다. config.yaml은 git 비추적이라 봇이 실거래 차단을 확정하려면 사용자가 trading.record_only: true를 로컬 파일에 추가해야 한다.
  </what-built>
  <how-to-verify>
    1. `git diff scripts/alt_monitor.py`로 매매 함수 3곳의 변경을 검토한다 — CLAUDE.md 규칙: 매매 코드 변경은 사용자 검토 후 진행.
       - 확인: 각 게이트가 함수 첫 줄이고, 기존 신호/필터 로직은 그대로인가?
    2. config.yaml을 열어 `trading:` 섹션에 `record_only: true`를 추가한다 (섹션이 없으면 신설). config.yaml은 절대 git에 커밋하지 않는다.
    3. 봇을 실행한다: `python scripts/alt_monitor.py` (또는 평소 기동 방식). 로그(`logs/alt_monitor.log`)에서 시작 직후 에러가 없는지 확인.
    4. 펌핑 신호가 잡히는 시점을 기다리거나, 검증을 빠르게 하려면 일회성으로 `do_buy`를 직접 호출하는 테스트를 돌려 "RECORD_ONLY 차단됨" 로그가 찍히는지 확인한다.
    5. 기대 결과: 신호는 평소처럼 감지·로깅되지만 시장가 매수/매도 API 호출은 0회. 로그에 "RECORD_ONLY 차단됨"이 신호마다 남는다.
  </how-to-verify>
  <resume-signal>매매 코드 변경을 검토하고 config.yaml에 record_only: true를 설정한 뒤 "approved" 입력, 또는 수정할 점을 알려주세요.</resume-signal>
</task>

</tasks>

<verification>
- `grep -c "RECORD_ONLY 차단됨" scripts/alt_monitor.py` 가 3
- 봇 실행 시 시장가 매수/매도 API가 호출되지 않고 로그에 차단 메시지 기록 (REC-01 성공기준 1)
- `_parse_exchange_ts`가 KST date/time을 epoch로 변환, getter가 코인별 시각 반환 (REC-03 입력단)
- 신호 감지·`signal_log`/`pump_log` 기록은 평소대로 동작 (D-03)
- deque 튜플 3원소 불변 — get_signal/get_preemptive_signal 무중단
</verification>

<success_criteria>
- RECORD_ONLY=true(기본값)일 때 do_buy/do_buy_limit/do_sell이 주문 없이 None 반환 + 차단 로그 (REC-01)
- WS가 거래소 발생 시각을 epoch로 보관하고 get_latest_exchange_ts로 조회 가능 (REC-03 토대 — Plan 03이 소비)
- 매매 코드 변경이 사용자 검토를 거침 (CLAUDE.md 규칙)
</success_criteria>

<output>
완료 후 `.planning/phases/01-tick-recording-infrastructure/01-02-SUMMARY.md`를 생성한다. SUMMARY에 "RECORD_ONLY 전환 전 active_pos.json 비우기" 운영 체크리스트와 get_latest_exchange_ts 시그니처(Plan 03 소비)를 명시한다.
</output>
